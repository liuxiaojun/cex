# 第 6 节 | 全链路串联：交易系统的暗线与闭环

> **核心命题：** 交易系统不是流水线，是闭环网络。每个服务既是上游的消费者，也是下游的生产者。前五节各拆一个子系统；这节把它们之间的"暗线"全部接通。

---

## 本节要回答的问题

| 序号 | 问题 |
|------|------|
| 一 | 教学拓扑和真实依赖有什么区别？隐藏的连线有哪几类？ |
| 二 | 反向流：Order Service 和 Matching Engine 之间的双向关系是什么？ |
| 三 | 旁路链路：Trigger Engine 和 Funding Service 各自怎么绕过主链路影响系统？ |
| 四 | 扇出与争用：一个状态更新同时被多方消费时会发生什么？ |
| 五 | 系统思维：改动一个字段，如何评估它的影响范围？ |

---

## 一、从教学拓扑到真实依赖

**开场衔接：** 前五节我们按 Account → Matching → Ledger → Risk → Wallet 的顺序，逐个拆解了每个子系统。拓扑图上画的是主路径 — 下单 → 撮合 → 清算 → 记账。但真实系统远不止这条线。

---

### 回顾：15 个核心服务

| 层 | 服务 |
|----|------|
| 接入层 | User Service, API Gateway, Wallet Service |
| 订单层 | Order Service |
| 核心决策层 | Account Service, Risk Service, Matching Engine, Position Service, Mark Price Service, Funding Service, Liquidation Engine |
| 结算/输出层 | Clearing Service, Market Data Service, Insurance Fund |
| 记录层 | Ledger Service |

> 前五节每节聚焦 1-2 个服务。但服务之间的连线 — 尤其是非主链路的连线 — 一直没有展开。这节补上。

---

### 教学拓扑 vs 真实依赖

```
┌──────────────────────────────────────────────────────────────────────┐
│                    教学拓扑（第 1 节）— 单向主路径                      │
│                                                                      │
│  用户 → API Gateway → Order Service → Risk Service → Matching Engine │
│                                                          ↓           │
│                                          Clearing → Account → Ledger │
│                                                                      │
│  特征：箭头全部朝一个方向                                              │
│  价值：帮你建立服务之间的"主链路"认知                                  │
│  缺陷：真实系统中，至少还有 6 条反向 / 旁路箭头没画出来                │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

```
┌──────────────────────────────────────────────────────────────────────┐
│                    真实依赖（补上暗线后）                              │
│                                                                      │
│  ═══ 主链路 ═══                                                      │
│                                                                      │
│  用户 → API Gateway → Order Service → Risk Service → Matching Engine │
│                                                                      │
│  ═══ Matching Engine 输出（raw execution event，三路分发）═══        │
│                                                                      │
│  Matching Engine ──→ Order Service（反向回流：更新订单状态）          │
│                  ──→ Market Data Service（行情推送）                  │
│                  ──→ Clearing Service（读 Order Snapshot 补全信息     │
│                          → 结算）                                     │
│                          → Account / Position / Ledger               │
│                          → Risk Service（结算后更新风险状态）         │
│                                                                      │
│  ═══ 旁路链路 ═══                                                    │
│                                                                      │
│  Trigger Engine ──监控 Mark/Last Price──→ Order Service ──→ 进入主链路│
│                                                                      │
│  Funding Service ──定时结算──→ Account Service（余额变动）            │
│      → Risk Service 重算 → Liquidation Engine → Matching Engine      │
│      → Clearing Service（完整链路）                                   │
│                                                                      │
│  ═══ 共享状态扇出 ═══                                                │
│                                                                      │
│  Mark Price Service ──→ Risk / Trigger Engine / Market Data（3路）   │
│  Risk Service ──→ Liquidation Engine（强平指令）                     │
│  Account Service ←── Order / Clearing / Funding / Liquidation /      │
│                       Wallet / 用户划转（6 路并发写入）               │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

| | 教学拓扑 | 真实依赖 |
|---|---------|---------|
| **方向** | 单向：下单 → 撮合 → 清算 → 记账 | 双向：Clearing 回写 Account，Risk 反向拦截 Order |
| **路径** | 一条主链路 | 主链路 + 多条旁路（Trigger Engine、Funding Service） |
| **状态** | 每个服务管自己的状态 | 关键状态被多个服务共享读写（Mark Price、Account 余额） |

> 教学拓扑是正确的 — 但它只画了"晴天路径"。暗线在异常、定时任务、反向回调中。

---

### 三类隐藏连线

| 类别 | 定义 | 典型例子 |
|------|------|---------|
| **反向流** | 下游产出回流到上游，更新上游状态 | Matching → Order（更新订单状态）、Clearing → Risk（更新风险状态） |
| **旁路链路** | 不经过用户触发的独立路径，由系统内部事件驱动 | Trigger Engine → Order → Matching、Funding → Account → Risk → Liquidation |
| **共享状态** | 服务间不通过直接调用，而是通过中间存储传递数据 | Order Snapshot（Redis）由 Order Service 写、Clearing Service 读取；Mark Price 扇出给多个消费者 |

---

### 暗线为什么重要

| | 明线（主链路） | 暗线（反向流 / 旁路 / 共享状态） |
|---|---|---|
| **特征** | 单向、同步或顺序消费、请求-响应模式 | 双向、异步、事件驱动、多消费者 |
| **典型问题** | 性能和可用性 — 服务挂了、延迟高了 | 竞态条件、消息乱序、一致性、级联故障 |
| **排查难度** | 相对简单 — 沿着链路从上往下查 | 极其困难 — bug 的表现和根因可能在完全不同的服务中 |

> 经验法则：系统上线后的 bug，80% 出在暗线上。因为明线是你设计时重点关注的路径，暗线是你以为"不会出问题"的路径。

---

## 二、反向流

### Order Service 的双向角色

**角色 1 — 生产者（明线）：**

```
用户提交: BUY 2 BTC @ 50,000
    → Order Service 创建订单
    → Account Service 冻结 100,000 USDT
    → Risk Service Pre-trade 检查 → pass
    → 提交 Matching Engine → 任务完成？
```

如果故事到此结束：用户问"我的单成交了没？" → Order Service："不知道，我把单交出去之后就没管了" → 不可接受。

**角色 2 — 消费者（暗线）：**

```
Matching Engine 输出 execution event（三路分发）：
    → Clearing Service   （明线：读 Snapshot 补全 → 结算）
    → Market Data Service（明线：行情推送）
    → Order Service      （暗线：更新订单状态）

Order Service 收到 Trade 后：
    filled_qty += trade.qty
    remaining_qty -= trade.qty
    → remaining_qty == 0 → status = FILLED
    → remaining_qty > 0  → status = PARTIALLY_FILLED
    → 更新订单记录（DB + 缓存）→ 推送给用户（WebSocket）
```

> Order Service 同时是 Matching Engine 的上游（提交订单）和下游（接收 Trade 事件更新状态）。这就是典型的双向依赖 — 反向流的核心特征。

---

### 撤单竞态 — Cancel Ack vs Last-Moment Fill

撤单是反向流复杂度的集中体现。

**正常撤单流程：**

```
1. 用户请求撤单: order_id = ORD-001
2. Order Service → Matching Engine: 请求从 Orderbook 移除
3. Matching Engine → Order Service: 返回 Cancel Ack
4. Order Service → Account Service: 解冻剩余冻结金额
5. Order Service 更新订单状态: CANCELLED
```

看起来简单？考虑这个场景：

**场景：** ORD-001: BUY 2 BTC @ 50,000，冻结 100,000 USDT，用户请求撤单。

```
关键前提：Matching Engine 单线程，对同一份数量不会既撮合又撤销。
但撤单请求有网络传输时间 — 在到达之前，订单可能被部分撮合。

T1  Order Service → Matching Engine: 撤销 ORD-001
T2  撤单请求还在传输中... 一笔卖单到达，匹配了 1 BTC
    → 输出 Trade: 成交 1 BTC @ 50,000 (seq=5)
T3  撤单请求到达，剩余 1 BTC → 从 Orderbook 移除
    → 输出 Cancel Ack: cancelled_qty=1 (seq=6)
```

两个事件通过不同路径到达 Order Service — 到达顺序不确定：

- **情况 A**：Trade(seq=5) 先到 → Cancel Ack(seq=6) 后到
  - Trade: filled=1 → 更新 filled_qty=1
  - Cancel Ack: cancelled=1 → 解冻 1×50,000 = 50,000 USDT
  - 正确：成交 1 BTC 消耗 50,000，解冻 50,000

- **情况 B（危险）**：Cancel Ack(seq=6) 先到 → Trade(seq=5) 后到
  - Order Service 本地 filled_qty=0（Trade 还没到）
  - Cancel Ack → "剩余冻结全部解冻" → 解冻 100,000 USDT（全量！）
  - 然后 Trade 到达：成交 1 BTC → 需要扣 50,000 → 但冻结已经是 0
  - → **资金状态不一致**

**设计要点：**

| 层次 | 设计 | 作用 |
|------|------|------|
| **基础** | 同 symbol 单通道有序投递（如 Kafka partition per symbol） | 从根源消除乱序，Trade 一定先于 Cancel Ack 到达 |
| **防御 1** | Cancel Ack 携带 cancelled_qty，解冻量 = cancelled_qty × price | 事件自包含，不依赖本地状态，架构变更也不会算错 |
| **防御 2** | 状态机终态保护（FILLED / CANCELLED 后拒绝变更） | 兜底：未预期的消息不会破坏已完结订单 |

> 核心是第一层：Matching Engine 同一 symbol 的输出天然有序，走单一有序通道就能消解竞态。后两层是纵深防御。

---

### Clearing Service 如何获取完整信息 — Order Snapshot 模式

Matching Engine 输出的 raw execution event 只包含撮合最小信息：

```
{
  trade_id: "T-20250312-001",
  order_id_maker: "ORD-A",
  order_id_taker: "ORD-B",
  price: 50000,
  qty: 1.5,
  side: BUY,          // taker 方向
  timestamp: 1710000000
}
```

Clearing Service 做结算时还需要手续费等级、reduce_only 标记、用户 account_id 等 — raw event 中全都没有。怎么补齐？

| 方案 | 做法 | 问题 |
|------|------|------|
| Clearing 同步调用 Order Service API | 每笔成交时查订单信息 | 同步调用引入延迟，高并发下 Order Service 成瓶颈，服务耦合 |
| Matching Engine 携带所有字段 | event 中包含 fee_rate、user_id 等 | 违反第 2 节的职责边界 — 撮合引擎不应持有业务属性 |
| **Order Snapshot** | Order Service 写快照到 Redis → Clearing Service 收到 raw event 后读快照补全 | **推荐** — Matching Engine 保持纯粹，Clearing 无需同步调用 Order Service |

**完整链路：**

```
Order Service ──写快照──→ Redis (Order Snapshot)
                                    ↑ 读取
Matching Engine ──raw event──→ Clearing Service（读 Snapshot → 补全 → 结算）
                                    ↓ 结算后输出
                              Account / Position / Ledger
                              Risk Service（更新风险状态）
```

---

### Order Snapshot 写入时序

Order Snapshot 的核心风险：**写快照和撮合成交是两个独立事件，时序不可假定。**

```
错误顺序（先提交撮合，再写快照）：
  T1  Order Service → Matching Engine: 提交订单
  T2  Matching Engine 立即成交 → 输出 Trade
  T3  Clearing Service 收到 raw event → Redis 中没有快照 → 补全失败！
  T4  Order Service → Redis: 写入快照（太晚了）

正确顺序（先写快照，再提交撮合）：
  T1  Order Service → Redis: 写入快照
  T2  Order Service → Matching Engine: 提交订单
  T3  Matching Engine 成交 → 输出 Trade
  T4  Clearing Service 收到 raw event → Redis 读到快照 ✓
```

> 因果关系保证：写快照 → 提交撮合 → 成交 → event 到达 Clearing → 读快照。快照一定在 raw event 之前写入。

---

### Order Snapshot 内容与生命周期

```
Redis Key: order_snapshot:{order_id}
TTL: 24h（超过 24 小时未成交的订单快照自动过期 → 回源查 DB）

Snapshot 内容:
{
  order_id:       "ORD-001",
  user_id:        "U-12345",
  account_id:     "ACC-FUTURES-12345",
  symbol:         "BTC-USDT",
  side:           "BUY",
  order_type:     "LIMIT",
  price:          50000,
  original_qty:   2.0,
  fee_tier:       "VIP3",
  maker_fee_rate: 0.0002,       // 0.02%
  taker_fee_rate: 0.0005,       // 0.05%
  reduce_only:    false,
  is_liquidation: false,
  source:         "API",        // 订单来源: API / Trigger
  created_at:     1710000000
}
```

| 阶段 | 说明 |
|------|------|
| **创建** | Order Service 写入 Redis（提交撮合前） |
| **使用** | Clearing Service 按 order_id 读取（每次成交时） |
| **更新** | 无 — 快照是创建时的不可变镜像，不随订单状态变化更新 |
| **删除** | 不主动删除，由 TTL 自然过期回收。原因：Order Service 和 Clearing Service 都消费 Matching Engine 的事件，Order Service 处理终态时无法保证 Clearing 已读完快照 |

---

### Raw event 缺什么 — Snapshot 补什么

| Raw execution event 包含 | Clearing Service 从 Order Snapshot 补全 |
|--------------------------|----------------------------------------|
| orderId | 订单类型（Limit / Market / IOC...） |
| matchPrice | 手续费费率（Maker / Taker） |
| matchQty | 用户 accountId |
| buyOrderId / sellOrderId | reduce_only 标记 |
| timestamp | 原始下单价格（计算滑点用） |

---

### Clearing Service 结算后的下游扇出

```
Clearing Service（读 Snapshot → 补全 → 结算）
    → Account Service（可用余额增减、冻结释放）
    → Position Service（仓位方向、数量、入场价更新）
    → Ledger Service（写入不可变流水）
    → Risk Service（更新风险状态、重算保证金率）
    → Insurance Fund（穿仓时注入剩余保证金）
```

> Clearing Service 消费 raw event 后，读 Snapshot 补全信息、完成结算，再扇出给 5 个下游服务。Risk Service 的 post-trade 输入来自 Clearing 的结算结果。

---

## 三、旁路链路

### 触发单是什么

| 类型 | 触发条件 | 用途 |
|------|---------|------|
| 止损单（Stop-Loss） | 价格 ≤ trigger_price（多头） | 价格跌到某位置 → 自动卖出止损 |
| 止盈单（Take-Profit） | 价格 ≥ trigger_price（多头） | 价格涨到某位置 → 自动平仓获利 |
| 条件单（Conditional Order） | 价格满足自定义条件 | 满足条件 → 自动下单（开仓或平仓） |

> 这里的"价格"由用户在创建触发单时选择：**Mark Price** 或 **Last Price**（最新成交价）。Mark Price 抗操纵，适合止损/止盈；Last Price 响应更快，适合追踪实时行情的条件单。交易所通常默认 Mark Price，但允许用户切换。

共同点：**不是立刻进入撮合，而是等条件满足后再变成真正的订单**。在条件满足之前，它们存在于 Trigger Engine 的监控列表中，不占用 Matching Engine 的任何资源。

---

### Trigger Engine — 条件单的完整生命周期

**阶段 1：创建（用户触发）**

```
1. 用户提交触发单: STOP_LOSS SELL 1 BTC, trigger_price=48,000
2. Order Service 校验参数:
   • trigger_price 是否合理（不低于当前价 20%）
   • 是否已有相同触发条件的单（防重复）
3. Order Service 记录触发单: status = WAITING
   注意: 不提交撮合，不冻结余额
4. Order Service → Trigger Engine: 注册触发单
```

**阶段 2：监控（系统持续运行）**

```
5. 价格源持续推送:
   • Mark Price Service → Mark Price topic
   • Market Data Service → Last Price topic
6. Trigger Engine 同时订阅两个 topic，每次价格变动:
   按触发单的 trigger_price_type 匹配对应价格源
   BTC Mark Price = 48,100 → 未触发（48,100 > 48,000）
   BTC Mark Price = 47,950 → 触发！（47,950 ≤ 48,000）
```

**阶段 3：触发 → 转为真正的订单（系统自动执行）**

```
7. Trigger Engine → Order Service: 通知触发 order_id=ORD-T001
8. Order Service 将触发单转为真正的订单:
   → Account Service:  冻结余额（此时才冻结）
   → Risk Service:     Pre-trade 检查
   → Matching Engine:  提交撮合（通常以市价单形式）
9. 后续链路同正常订单:
   Matching → Clearing → Account / Position / Ledger
```

**失败可能：** 步骤 8 中 Account Service 余额不足 → 冻结失败，或 Risk Service Pre-trade 检查不通过 → 触发单变为 FAILED → 用户的止损没有执行。这是"创建时不冻结"的代价。

---

### Trigger Engine 的系统位置与边界

```
Mark Price Service    Market Data Service
     │                      │
     │ Pub/Sub              │ Pub/Sub
     │ (mark price topic)   │ (last price topic)
     └──────────┬───────────┘
                ↓
┌──────────────────────────────────────┐
│        Trigger Engine                 │
│                                       │
│  输入: Mark Price + Last Price 价格流 │
│  内部: 维护 WAITING 触发单列表        │
│  输出: 触发事件 → Order Service       │
│                                       │
│  ┌─────────────────────────────────┐ │
│  │ 触发单索引（按 trigger_price）  │ │
│  │                                 │ │
│  │ BTC-USDT:                       │ │
│  │   STOP_LOSS (sell):             │ │
│  │     48,000 → [ORD-T001]        │ │
│  │     45,000 → [ORD-T002, T003]  │ │
│  │   TAKE_PROFIT (sell):           │ │
│  │     55,000 → [ORD-T004]        │ │
│  │     60,000 → [ORD-T005]        │ │
│  └─────────────────────────────────┘ │
└──────────────────────────────────────┘
     │
     │ 触发事件
     ↓
Order Service → Account → Risk → Matching Engine
(正常交易链路)
```

| 职责 | Trigger Engine 做 | Trigger Engine 不做 |
|------|------------------|-------------------|
| 监控价格 | ✓ 接收 Mark Price + Last Price | ✗ 不自己计算价格 |
| 判断是否触发 | ✓ 比较价格与触发条件 | ✗ 不做风控检查 |
| 执行下单 | ✗ 通知 Order Service | ✗ 不直接提交撮合 |
| 冻结余额 | ✗ | ✗ 由 Order Service 在触发后调用 Account Service |

🔸 **案例提示：** 如果有 10 万笔 WAITING 状态的触发单，每次价格更新都遍历一遍不现实。常见优化：按 trigger_price_type 和触发价格分别建索引（类似第 4 节 Risk Service 的排序结构），价格变动时只检查对应类型中"已经越过触发线"的子集。止损单按 trigger_price 从高到低排序（多头），价格下跌时从顶部扫描到安全位置即可停止——复杂度从 O(N) 降为 O(K)。

> Trigger Engine 是"旁路注入器"：平时不在主链路上，一旦触发就向主链路注入流量。

---

### Funding Service — 定时结算的级联效应

**直接效果（第 1 节已覆盖）：**

```
1. Funding Service 计算各用户的 Funding 应付/应收
   BTC-USDT Funding Rate = 0.01%
   user_A: long 10 BTC, 名义价值 500,000 USDT
   Funding 费用 = 500,000 × 0.01% = 50 USDT（多头付费）

2. Funding Service → Account Service: user_A available -= 50
3. Funding Service → Ledger Service:  写入 Funding 账本
4. Funding Service → Risk Service:    通知余额变更
```

**连锁效应：**

```
假设 user_A 的状态（Funding 前）:
  available: 60 USDT, margin_used: 25,000, unrealized_pnl: -24,500
  equity = 60 + 25,000 - 24,500 = 560 USDT
  margin_ratio = 560 / 500 = 1.12（维持保证金 = 500）→ 已接近强平线（1.0）

Funding 扣费后:
  available: 60 - 50 = 10 USDT
  equity = 510, margin_ratio = 1.02 → 岌岌可危

此时 BTC 再跌 0.1%:
  unrealized_pnl: -25,000 → equity = 10 → margin_ratio = 0.02
  → 远低于 1.0 → 触发强平！

5. Risk Service 检测到 margin_ratio < 1.0
6. Risk Service → Liquidation Engine: 发出强平信号
7. Liquidation Engine → Matching Engine: 强平单进入撮合
8. Matching Engine → Clearing Service: 结算
9. Clearing Service → Account / Position / Ledger: 完成清算
```

> Funding Service 只做了一件事：扣了 50 USDT。它不知道这 50 USDT 是 user_A 活下来的最后缓冲。但从系统视角，这 50 USDT 触发了完整的 Risk → Liquidation → Matching → Clearing 链路。

---

### Trigger Engine vs Funding Service 对比

| | Trigger Engine | Funding Service |
|---|---------------|----------------|
| **起点** | Mark Price / Last Price 变动（事件驱动） | 定时器（每 8 小时，时间驱动） |
| **是否经过用户交互** | 否 — 系统内部自动执行 | 否 — 定时任务自动执行 |
| **最终汇入** | Order → Matching → Clearing | Account → Risk → Liquidation → Matching → Clearing |
| **极端情况风险** | 价格瀑布式下跌 → 大量止损单同时触发 → 流动性冲击 → 踩踏 | Funding 扣费 → 批量用户保证金不足 → 连环清算 |
| **前 5 节是否覆盖** | 未提及 Trigger Engine | 仅提及 Funding 基本流程，未展开连锁效应 |
| **与第 4 节的关系** | 大量触发单涌入 Matching Engine ≈ 第 4 节讲的 Death Spiral | Funding 触发的强平 = 第 4 节的 In-trade 强平的一个输入源 |

> 两条旁路链路的共同特征：**它们不经过用户触发，但能启动完整的交易链路甚至触发连环清算——它们是系统中最容易被忽视的风险源。**

---

## 四、扇出与争用

### Mark Price 扇出

Mark Price 由 Mark Price Service 计算（聚合外部 Oracle + 内部 Last Price），单一来源，三个消费者：

```
              Mark Price Service
              (计算标记价格)
                    │
     ┌──────────────┼──────────────┐
     │              │              │
     ↓              ↓              ↓
Risk Service   Trigger Engine  Market Data Service
• 重算所有      • 检查 type=    • 推送标记价格
  持仓用户的      Mark 的触发单    给用户前端
  unrealized PnL• 还额外消费
• 判断是否强平    Last Price
  → 触发后发
    强平指令给
    Liquidation
    Engine
```

> Liquidation Engine 不直接消费 Mark Price — Risk Service 是强平的决策者，Liquidation Engine 是执行者。Trigger Engine 还额外消费 Last Price（来自 Market Data Service）来处理 trigger_price_type=Last 的触发单。

### Mark Price 延迟容忍度

| 消费者 | 延迟容忍度 | 迟到的后果 |
|--------|-----------|-----------|
| Risk Service | 极低（毫秒级） | 强平判断延迟 → 穿仓，Insurance Fund 买单 |
| Trigger Engine | 低（毫秒-十毫秒级） | 止损单延迟触发，用户损失扩大 |
| Market Data Service | 高（百毫秒级） | 展示延迟，不影响系统正确性 |

> 同一个数据源，分发给不同消费者时需要不同的延迟保障 — 这是扇出设计的核心难题。

### Mark Price 异常的级联影响

**场景 1：Mark Price 延迟**

```
Oracle 数据源之一宕机 → Mark Price 更新延迟 3 秒
    → Risk Service:      用旧价格判断 → 本该强平的用户没被检测到
    → Trigger Engine:    用旧价格判断 → 止损单延迟触发
    → Market Data:       用户看到的标记价格是 3 秒前的
    → 延迟期间价格继续恶化 → 等 Mark Price 恢复时 → 穿仓面积已经扩大
```

**场景 2：Mark Price 异常跳变**

```
某个 Oracle 数据源报出错误价格 → Mark Price 聚合后偏离真实价格 5%
    → Risk Service:      误判 → 不该强平的用户被强平
    → Trigger Engine:    误触发大量止损单
    → 止损卖单涌入 Matching Engine → 实际市场价格被砸低
    → Mark Price 修正后，被误强平的用户已经损失了
```

> Mark Price 出问题 = 三条链路同时出问题。这就是为什么 Mark Price 需要多源聚合、异常剔除、Median 保护（第 4 节）。

---

### Account Service — 6 个并发写入源

Account Service 是系统中被写入最频繁的服务。以下 6 个来源可能同时修改同一个用户的余额：

| 写入源 | 操作 |
|--------|------|
| Order Service | 下单冻结: available -= X, frozen += X |
| Clearing Service | 成交结算: frozen -= X, 对手方 available += X |
| Funding Service | Funding 扣费: available -= Y |
| Wallet Service（充值） | 充值入账: available += Z |
| Wallet Service（提现） | 提现冻结: available -= W, frozen += W |
| Liquidation Engine | 强平结算: 通过 Clearing Service 清算保证金和仓位 |

### 竞态条件 — 丢失更新

```
user_A available = 1,000 USDT

T1  [Clearing Service]  读取 available = 1,000
    准备写入: available = 1,000 + 500 = 1,500（成交获利 500）

T2  [Funding Service]   读取 available = 1,000
    准备写入: available = 1,000 - 100 = 900（Funding 扣费 100）

T3  [Clearing Service]  写入 available = 1,500
T4  [Funding Service]   写入 available = 900  ← 覆盖了 Clearing 的写入！

正确结果: 1,000 + 500 - 100 = 1,400
实际结果: 900
差额: 500 USDT 凭空消失

反过来也一样危险:
T3 和 T4 顺序反过来 → 实际 1,500，正确应该是 1,400
→ Funding 扣费"没生效" → 交易所资产负债表不平
```

### 并发控制方案对比

| 方案 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| **数据库行锁（悲观锁）** | `SELECT ... FOR UPDATE` 同一行串行 | 实现简单，与第 1 节一致；锁粒度 per-user，不同用户完全并行 | 高频交易用户（如做市商）可能成为瓶颈 |
| **乐观锁 + 版本号（CAS）** | 写入时 `WHERE version=N`，冲突则重试 | 无锁等待，低冲突时吞吐高 | 高冲突时大量重试，需要写重试逻辑 |
| **Per-user 事件队列** | 余额变更投递到 per-user 队列，单线程消费 | 彻底消除竞态，天然幂等 | 引入异步，调用方需要等回调确认 |

> 同一张余额表的同一行，只能选一种并发控制策略。对 Account 余额更新而言，数据库行锁是最直接的选择 — 锁粒度天然 per-user，同一用户并发写入频率通常不高，实现简单，与第 1 节一致。如果单用户写入并发确实成为瓶颈 → 再考虑 per-user 事件队列。

---

## 五、系统思维 — 一个字段变更的影响评估

### 案例：给订单增加 `reduce_only` 字段

`reduce_only = true` 表示"只允许减仓，不允许开新仓"。看起来只是一个 boolean 字段，估时 2 天？

**实际影响链：**

| 服务/组件 | 影响 |
|-----------|------|
| **Order Service** | 新增字段 → API 入参/出参变更 → DB schema 加列 |
| **Order Snapshot** | Redis 快照结构必须同步加入 reduce_only。不改 → Clearing Service 读到的快照缺字段 |
| **Clearing Service** | 结算时需要新增分支：reduce_only 成交后只减仓不开新仓 |
| **Risk Service** | Pre-trade 检查新增逻辑：reduce_only 单不检查初始保证金，但需检查用户是否有可减仓位 |
| **Trigger Engine** | 触发单转正式单时 reduce_only 标记必须透传，不能丢 |
| **Liquidation Engine** | 需要讨论：强平单天然是减仓的，但如果代码统一检查 reduce_only 则需正确设置 |
| **Matching Engine** | 不受影响 — 不关心业务属性（第 2 节边界设计在此体现价值） |
| **Market Data / Ledger** | 不受影响 |

> 受影响 5 个，不受影响 4 个，需要讨论 1 个。如果只改了 Order Service 就上线 → Snapshot 里没有 reduce_only → Clearing 读不到 → 结算逻辑出错。

---

### 三问评估框架

当你要修改系统中的任何一个字段、逻辑、或行为时：

| 问题 | 检查方向 | 示例（Order Service 加 reduce_only） |
|------|---------|--------------------------------------|
| **谁消费我的输出？** | 下游明线 + 下游暗线 | Matching Engine（明线）、Clearing Service 读 Order Snapshot（暗线） |
| **谁的输出会流回我？** | 上游反向流 | Matching → Order 的 Trade 事件、Trigger Engine → Order 的触发事件 |
| **我的数据被谁共享？** | 共享状态 | Redis 中的 Order Snapshot（Clearing Service 读取）、Order 状态变更（WebSocket 推送给用户） |

**通用适用：**

```
改 Account Service 的余额字段?
  Q1: 谁消费? → Risk, Order, Clearing, Funding, Wallet = 5 个
  Q2: 谁回流? → Clearing 结算后回写余额
  Q3: 谁共享? → Risk Service 可能缓存了用户余额的快照

改 Mark Price Service 的计算逻辑?
  Q1: 谁消费? → Risk, Trigger, MarketData = 3 个
  Q2: 谁回流? → Oracle 数据源
  Q3: 谁共享? → 所有消费者通过 Pub/Sub 获取同一份价格
```

> 经验法则：你以为改动只影响 1 个服务 → 实际最少影响 3 个。你以为影响 3 个 → 实际可能是 5 个。原因：暗线上的依赖关系在代码中不显式可见。

---

## 全景回顾：主链路 + 暗线

### 主链路（正向）

```
用户 → API Gateway → Order Service → Account Service（冻结）→ Risk Service（检查）→ Matching Engine → Clearing Service（读 Snapshot 补全 → 结算）→ Ledger Service
```

### 反向流

```
Trade 事件 → Order Service（更新状态）
Clearing → Account Service（余额回写）
Clearing → Position Service（仓位回写）
Clearing → Risk Service（状态重算）
```

### 旁路链路

```
Trigger Engine ──监控价格──→ Order Service ──→ 主链路
Funding Service ──定时──→ Account Service ──→ Risk Service ──→ Liquidation ──→ Matching ──→ Clearing
Wallet Service ──充值──→ Account Service
```

### 共享状态扇出

```
Mark Price Service ──→ Risk / Trigger Engine / Market Data（3 路扇出）
Risk Service ──→ Liquidation Engine（强平指令）
Account Service ←── Order / Clearing / Funding / Liquidation / Wallet / 用户划转（6 路写入）
```

> 把这四张图叠在一起 — 就是真实系统的依赖网络。

---

## 本节判断点汇总

1. **交易系统不是流水线，是闭环网络。** 教学拓扑只画了正向主链路；真实系统中反向流、旁路链路、共享状态才是复杂度的主要来源。
2. **Order Snapshot 模式隔离了复杂度。** Matching Engine 保持纯粹（不持有业务属性），Clearing Service 通过读 Redis 快照补全信息后直接结算——无需额外服务节点，无同步调用。
3. **旁路链路能触发主链路。** Trigger Engine 和 Funding Service 平时不在主链路上，但它们注入的订单或资金变动会走完整的撮合-清算流程。
4. **共享状态的并发写入是系统复杂度的根源。** Account Service 有 6 个并发写入源 — 一致性保障不是"可选优化"，而是正确性前提。
5. **评估任何变更都要问三个问题：谁生产、谁消费、消费后触发什么。** 闭环网络中没有"局部修改"。

---

## 回扣核心命题：为什么说"交易系统不是流水线，是闭环网络"

1. **反向流打破了单向假设** — Order Service 既是 Matching Engine 的上游（提交订单），也是它的下游（接收 Trade 事件更新状态）。Clearing Service 结算后回写 Risk Service 更新风险状态。每个"下游"都在反向影响"上游"
2. **旁路链路引入了新的起点** — 不是所有链路都从用户开始。Trigger Engine 由 Mark Price / Last Price 驱动，Funding Service 由定时器驱动——它们能独立启动完整的交易链路，甚至触发连环清算
3. **共享状态创造了隐含耦合** — Order Snapshot 在 Redis 中被 Order Service 写、Clearing Service 读。Mark Price 通过 Pub/Sub 被 3 个消费者同时消费。这些耦合关系在代码中不显式可见，只有在出 bug 时才会暴露
4. **扇出和争用是性能瓶颈的根源** — Mark Price 的 3 路扇出决定了它是全系统最关键的单点；Account Service 的 6 路写入决定了它是全系统最大的争用点

### 与前 5 节的关系

| 节次 | 当时的视角 | 这节课揭示的暗线 |
|------|-----------|-----------------|
| 第 1 节（资金系统） | Account Service 是"资金枢纽" | 同时被 6 个服务写入的并发争用问题 |
| 第 2 节（撮合引擎） | Matching Engine 不关心业务属性 | 这个边界设计在 Order Snapshot 方案中体现价值 |
| 第 3 节（账本） | Ledger 是所有变更的最终记录 | 不管经过多少暗线，最终都落入 Ledger |
| 第 4 节（风控） | Risk Service 做 Pre-trade 检查 | 消费 Mark Price、Clearing 回流、Funding 回流——暗线最密集的服务之一 |
| 第 5 节（钱包） | Wallet Service 管理链上资产 | 充值/提现写入 Account Service——6 路写入中的两路 |

---

**下节预告：** 前六节讲完了 CEX 的完整架构。从下一节开始进入 DEX — 当这些服务不再由交易所统一运行，而是分散到合约、链和用户身上时，信任模型会发生什么根本变化？ → 第 7 节：从 CEX 到 DEX：信任模型与架构重构
