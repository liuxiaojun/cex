# 第 4 节 | 风控为什么必须嵌在交易流程中

> **核心命题：** 风险一旦发生就不可逆。事后风控几乎等于没有风控。风控的价值，取决于它在系统结构中站的位置。

---

## 本节要回答的问题

| 序号 | 问题 |
|------|------|
| 一 | Risk Service 的完整职责是什么？它和 Liquidation Engine 的边界在哪里？ |
| 二 | Pre-trade 检查为什么必须同步阻塞？检查链的顺序为什么不能乱？ |
| 三 | In-trade 强平的触发依据为什么不能是最新成交价？强平单如何快速进入撮合？ |
| 四 | Risk Service 为什么需要维护实时风险状态？这个状态由什么驱动更新？ |
| 五 | 风控本身可能成为系统风险源吗？如何应对？ |

---

## 一、Risk Service 的完整职责与边界

**开场衔接：** 上节课 — "事后兜底靠账本"。这节课 — "有些风险等不到事后"。

---

### 本节课的范围：合约风控

**现货和合约都有完整的风控体系，但关注的风险维度完全不同。本节课聚焦合约风控。**

| 现货风控重心 | 合约风控重心 |
|------------|------------|
| 账户安全（反欺诈） | 保证金管理 |
| 提现风控（限额/白名单/多签） | Pre-trade margin check |
| 交易风控（刷量/操纵/STP） | In-trade 强平监控 |
| AML / Chain analysis | Mark Price 驱动的实时状态 |
| 系统级风控（暂停交易/提现） | Insurance Fund + ADL 兜底 |

合约的核心问题：**杠杆导致亏损可能超过本金** → 必须有独立的 Risk Service 持续监控。

> 后面所有讨论 — Pre-trade、In-trade、Mark Price 架构 — 都是合约语境。

---

### Risk Service ≠ Pre-trade 门卫

Risk Service 是持续运行的"风险大脑"，Pre-trade 检查只是它的一个输出。

**核心能力：为每个用户维护实时风险画像**

```
用户风险画像（per user）:
  各交易对的持仓、保证金占用、未实现盈亏
  → 聚合为: 总敞口、总保证金使用率、距强平线距离

Pre-trade 基于它 → "这个用户还能不能再开仓？"
In-trade 基于它  → "这个用户是不是快要爆仓了？"
```

---

### 两个输出方向

```
输出 1 — Pre-trade（逐单判断）:
  Order Service → Risk Service → pass/reject → Matching Engine
  触发时机: 每笔订单进入撮合前
  性质: 同步阻塞

输出 2 — In-trade（强平信号）:
  Mark Price 变动 → Risk Service 更新风险状态 → 发现保证金率越线
  → 发出强平信号 → Liquidation Engine 执行
  触发时机: 持续运行
  性质: 事件驱动
```

---

### Risk Service vs Liquidation Engine

| | Risk Service | Liquidation Engine |
|--|--|--|
| 角色 | 大脑（决策） | 手（执行） |
| 做什么 | 判断"谁需要被强平" | 生成强平单，提交给 Matching Engine |
| 持有状态 | 全局风险状态表 | 不持有状态，接收指令即执行 |
| 输入 | Mark Price、成交回报、Funding 回报 | Risk Service 的强平信号 |
| 输出 | Pre-trade: pass/reject; In-trade: 强平信号 | 强平订单 → Matching Engine |

为什么分开？ — 强平执行失败不影响 Risk Service 继续监控其他用户。

---

### 其他职责

- **风控参数管理**：保证金率、杠杆范围、持仓上限；根据波动率动态调整
- **异常行为检测**：洗盘、幌骗等模式识别 → 反馈到 Pre-trade 规则

共同点：都是基于"实时风险状态"做判断，不执行任何资金操作。

---

### 强平价由谁计算？

强平价公式是确定性的 — 各服务根据各自需要独立计算，不存在"强平价计算中心"：

| | Risk Service | Position Service |
|--|--|--|
| 目的 | 内部强平监控 | 用户端展示 |
| 存储 | 内存排序结构 | 仓位记录（持久化） |
| 更新时机 | 仓位变动事件到达时 | 开仓/加仓/追加保证金时 |
| 逐仓结果 | 精确 liq_price | 精确 liq_price（一致） |
| 全仓多仓位 | 用户级 equity 快照 + delta 估算 | 预估值（UI 标注"预估"） |

为什么不让 Risk Service 统一算？ — Risk Service 是高频低延迟内部服务，加用户查询负载会影响风控性能；且产生耦合（Risk Service 挂了 → 仓位页面打不开）。公式确定性 → 各算各的，不需要依赖。

> Risk Service 是"判断者"不是"执行者" — 不冻结余额、不关仓位、不划转资金、不对外提供强平价查询。只做判断。

---

## 二、Pre-trade（事前风控）：基于风险状态的逐单判断

> 开头已明确：本节聚焦合约风控。Pre-trade 由 Risk Service 独立完成。

### 为什么 Pre-trade 必须同步阻塞

```
异步风控的灾难:

T1  订单直接发给 Matching Engine（风控还在异步检查）
T2  Matching Engine 撮合成交 → 输出 Trade
T3  Clearing 结算完成 → Account 更新 → Ledger 记账
T4  Risk Service 检查完成 → 发现"保证金不足"
T5  怎么回滚？→ 成交已发生、Clearing 已结算、Ledger 是 append-only

→ 回滚成本极高，某些场景下根本不可能
```

> Pre-trade 的"同步"不是性能妥协，而是正确性要求。异步风控等于"先开枪再瞄准"。

---

### 检查路由：开仓、平仓、强平走不同路径

```
pre_trade_check(order):
    // 开仓/加仓 → 全量检查
    if order is increasing position:
        if not check_margin(order):           reject("保证金不足")
        if not check_price_band(order):       reject("价格偏离过大")
        if not check_position_limit(order):   reject("超出持仓上限")
        if not check_tier_margin(order):      reject("跨档后保证金不足")

    // 平仓/减仓 → 最小化检查（不拦止损）
    if order is reducing position:
        if not check_price_band_loose(order): reject("价格偏离极端")

    // 强平单 → 跳过 Pre-trade，直接进撮合

    send_to_matching_engine(order)
```

核心原则：**拦住平仓单比放过它的风险更大** — 不能让风控阻止用户止损。

| 检查项 | 数据来源 | 拒绝率 | 路径 |
|--------|---------|--------|------|
| margin | 本地风险状态 | 中高 | 开仓 |
| price_band | 本地缓存市场价 | 中 | 开仓+平仓(宽松) |
| position_limit | 本地风险状态 | 低 | 开仓 |
| tier_margin | 本地风险状态 | 低 | 开仓 |

> rate_limit 在 API Gateway 处理，self_trade（STP）在 Matching Engine 内处理 — 不经过 Risk Service。

---

## 三、In-trade（事中风控）：持续监控与强平决策

### 强平该用什么价格触发？

**最新成交价（Last Price）可以被操纵：**

```
插针攻击:

1. 攻击者在流动性差的时段，用极少量资金下一笔偏离市场价的卖单
2. Last Price 瞬间偏离（如 50,000 → 45,000）
3. 如果用 Last Price 触发强平 → 大面积"误强平"
4. 攻击者在低价接盘 → 价格恢复 → 获利

攻击成本: 可能只需几万美元
攻击收益: 大量用户的强平仓位 → 可能数百万美元
```

**那直接用 Index Price 呢？**

```
Index Price = Σ(Weight_i × Spot_Price_i)  多个交易所现货加权平均
→ 单交易所插针无法影响 ✅

但 Index Price 是现货价格，忽略合约-现货的合理 basis:

  BTC 现货（Index Price）= 10,000
  BTC 永续合约 = 10,200（多头需求旺盛，合理溢价 2%）
  用户在 10,200 开多，10x 杠杆，保证金 = 1,020

  用 Index Price 触发强平:
    unrealized_pnl = (10,000 − 10,200) × 1 = −200 → 开仓瞬间就"亏"200 → 可能直接强平
    但用户是按合约公允价格开仓的，并没有真的亏

  用 Mark Price 触发强平:
    Mark Price ≈ 10,180（Index Price + 合理 basis）
    unrealized_pnl = (10,180 − 10,200) × 1 = −20 → 合理反映真实盈亏
```

**Mark Price 的计算（以 Binance USDT-M 为例）：**

| 层级 | 公式 | 解决什么问题 |
|------|------|-------------|
| 第一层 Index Price | Σ(Weight_i × Spot_Price_i) | 防单交易所操纵 |
| 第二层 Funding Rate | Premium Index + Clamp(Interest Rate − Premium Index, ±0.05%) | 量化合理 basis，Clamp 防极端 |
| 第三层 Mark Price | **Median**(Price 1, Price 2, Contract Price) | 任何单一信号被操纵都不会直接决定结果 |

```
Price 1 = Index Price × (1 + Last Funding Rate × (Time Until Next Funding / 8h))
          → Funding Rate 线性衰减加回合理 basis

Price 2 = Index Price + MA(30s Basis)
          MA = Σ[(Bid1_i + Ask1_i)/2 − PI_i] / 30
          Bid1_i / Ask1_i = 采样时刻的盘口最优买一/卖一价，PI_i = 采样时刻的 Price Index
          → 每秒采样，取 30 个点的移动平均

Contract Price = 最新合约成交价

取三者中位数 → 任何一个被操纵，另外两个把它拉回来
```

| 价格类型 | 防操纵 | 反映 basis | Median 兜底 | 适合触发强平 |
|----------|--------|-----------|------------|-------------|
| Last Price | ✗ | ✓ | — | ✗ |
| Index Price | ✓ | ✗ | — | ✗ |
| Mark Price | ✓ | ✓ | ✓ | ✓ |

> 强平触发价格是安全模型决策 — Last Price 暴露给攻击者，Index Price 忽略 basis 误杀高杠杆用户，Mark Price 通过三层机制（多源 Index → Clamp 约束的 Funding Rate → Median 取中位数）同时解决两个问题。

---

### In-trade 强平链路

```
[Mark Price Service]  价格变动
      ↓
[Risk Service]        更新风险状态 → 发现 user_A 保证金率 ≤ 维持保证金
      ↓
[Risk Service]        发出强平信号: {user: A, position: ..., reason: margin_breach}
      ↓
[Liquidation Engine]  接收信号 → 生成强平市价单（跳过 Pre-trade，路由更短）
      ↓
[Matching Engine]     强平市价单进入撮合，遵守价格优先·时间优先
      ↓
[Clearing Service]    结算 → 仓位关闭 → 剩余保证金注入 Insurance Fund / 穿仓由 Insurance Fund 补足
```

**强平单的"快"：** 市价单形式（进入即执行）+ 内部路由更短（跳过 Pre-trade）+ 不受 rate limit 约束。但进入 Matching Engine 后和所有订单一样遵守价格优先·时间优先 — 不存在"跳过队列"的特殊通道。极端行情下大量强平市价单涌入会快速消耗深度，加剧踩踏。

---

### ADL（自动减仓）— 风控的最后手段

```
强平单进入 Matching Engine
  ↓ 正常撮合成功 → 正常结算
  ↓ 撮合失败（流动性枯竭）→ 穿仓
  ↓ Insurance Fund 兜底
  ↓ Insurance Fund 不够 → 触发 ADL
  ↓
Risk Service 计算 ADL 排名（盈利率 + 杠杆倍数 → 盈利最多、杠杆最高的优先）
  ↓
Liquidation Engine 强制减少排名靠前用户的仓位
  ↓
被 ADL 的用户按 Mark Price 平仓

代价: 被 ADL 的用户是"无辜的" — 但不 ADL 则穿仓损失由系统承担
→ ADL 是"最不坏"的方案
```

---

## 四、Mark Price 驱动的实时风险状态架构

### 为什么维护实时状态，而不是每次从零计算

- **Pre-trade**：每次都从 Position Service + Account Service + Mark Price Service 实时拉取 → 延迟不可接受（Pre-trade 是同步阻塞环节）
- **In-trade**：不可能每秒对所有用户全量计算 — 大型交易所可能数十万用户同时持仓

**结论：** 实时风险状态 = 缓存 + 增量更新，兼顾速度和准确性。

但"增量更新"还不够 — Mark Price 每秒可能更新数次，每次理论上影响所有持仓用户。需要分两层优化：**架构层面**拆分并行，**算法层面**优化每个分区。

---

### Mark Price 更新时如何高效处理所有仓位

#### 架构层面：按仓位类型分区

```
Mark Price Service → publish per topic (Pub/Sub)

┌─ 分区一: 逐仓 + 全仓单仓位 ─────────────────────────────┐
│  仓位独立 → 按交易对再分片                                 │
│  BTC 线程 / ETH 线程 / SOL 线程 ...（各自 latest-wins）   │
│  预计算 liq_price + 排序结构 → O(K) 扫描 → 直接强平       │
│  完美并行，零跨线程依赖                                    │
└───────────────────────────────────────────────────────────┘

┌─ 分区二: 全仓多仓位 ────────────────────────────────────┐
│  仓位耦合（共享保证金池）→ 按用户维度处理                  │
│  按用户分片 → 各线程维护自己的反向索引 + equity 缓存 │
│  price tick → 查索引 → delta 估算 per user → 强平判断     │
│  自然处理多资产关联                                        │
└───────────────────────────────────────────────────────────┘
```

为什么按仓位类型分区，而不是按交易对分区？
- 逐仓/全仓单仓位 → 仓位独立 → 按交易对并行 → 完美
- 全仓多仓位 → 仓位耦合 → 按交易对分片后还要跨线程聚合 → 不如直接按用户维度处理

---

#### 分区一：逐仓 + 全仓单仓位 — 预计算强平价格

逐仓模式下仓位完全独立；全仓但只有一个仓位时，没有其他仓位的盈亏干扰，本质相同。

```
优化: 把"重算保证金率"变成"比较一个数字"

开仓时预计算强平价格（liq_price）:
  多头: liq_price = entry - (margin - maint_margin × value) / size
  空头: liq_price = entry + (margin - maint_margin × value) / size

Mark Price 更新时:
  多头: mark_price ≤ liq_price → 强平
  空头: mark_price ≥ liq_price → 强平
→ 每仓位 O(1) 比较

只在仓位本身变化时重算 liq_price（开仓/加仓/结算/参数调整）
→ 这些事件频率远低于 Mark Price 更新频率
```

再用有序结构消除遍历开销：

```
同一交易对所有仓位按 liq_price 从高到低排序:

  liq_price    user
  ─────────    ─────
  49,200       user_C    ← 最危险
  48,500       user_A
  47,800       user_F
  ...
  32,000       user_X    ← 最安全

Mark Price = 48,600 时:
  user_C (49,200 > 48,600) → 强平
  user_A (48,500 < 48,600) → 安全 → 停止扫描

→ O(N) 变成 O(K)，K = 需要强平的仓位数，正常行情下接近 0
→ 每个交易对线程维护自己的排序结构
→ 全部放内存（单交易对 20 万仓位 × 100 字节 ≈ 20 MB）
```

---

#### 分区二：全仓多仓位 — 用户级 Equity 追踪 + Delta 估算

多仓位共享保证金池时，一个仓位的盈亏影响其他仓位的强平条件 — 不能再按单仓位预计算精确强平价格。

```
问题:
  user_A 持有 BTC long + ETH short，共享保证金池
  BTC 跌 → BTC 仓位亏损 → 共享保证金池缩水
  → ETH 仓位虽然价格没变，但"能兜住它的钱"变少了
  → 判断单位从"仓位"变成"用户"：margin_ratio = total_equity / total_maintenance_margin
```

**核心方案：反向索引 + Delta 估算**

```
数据结构:
  反向索引: BTC-USDT → [user_A, user_D, ...]
  用户缓存: user_A { equity: 12,300, maint_margin: 3,500,
                      positions: [BTC long 2, ETH short 50] }

处理流程 — BTC 从 50,000 → 49,000:
  1. 查反向索引 → [user_A, user_D, ...]
  2. user_A: delta = (49,000 - 50,000) × 2 = -2,000
             equity' = 12,300 - 2,000 = 10,300
             10,300 / 3,500 = 2.94 → 安全
  3. user_D: delta = (49,000 - 50,000) × 5 = -5,000
             equity' = 8,200 - 5,000 = 3,200
             3,200 / 4,100 = 0.78 → 强平 ✓
  4. 更新各用户的 cached_equity
```

**多资产关联 — 天然处理：**

```
BTC 跌、ETH 涨几乎同时到达:
  T1: BTC 更新 → user_A equity: 12,300 → 10,300（缓存更新）
  T2: ETH 更新 → user_A equity: 10,300 + delta_ETH（基于最新缓存）

每次 delta 都基于最新 cached_equity（已包含之前所有资产变化）
→ 多资产同时亏损 → equity 持续下降 → 不会遗漏
```

Equity 缓存在仓位变动 / 结算 / 参数调整时完整重建（低频），高频路径只做 delta 加减法。

**分区二的并行策略 — 按用户分片：**

```
分区一按交易对分片 — 仓位独立，不同交易对线程互不干扰
分区二不能按交易对分片 — BTC 线程和 ETH 线程会并发写同一个用户的 equity
→ 按用户分片:

线程 1: 负责 user_A, user_B ...（各自维护 equity 缓存 + 反向索引）
线程 2: 负责 user_D, user_E ...
所有线程订阅所有交易对的 price topic

BTC 价格更新:
  线程 1: 查自己的反向索引 → user_A → delta 估算
  线程 2: 查自己的反向索引 → user_D → delta 估算
  → 完全并行，无锁

同一个原则: 找到数据独立的维度来分片
  分区一 → 交易对维度独立
  分区二 → 用户维度独立
```

---

#### 两个分区对比

| | 分区一（逐仓 + 全仓单仓位） | 分区二（全仓多仓位） |
|--|--|--|
| 仓位关系 | 独立 | 耦合（共享保证金池） |
| 判断单位 | 仓位 | 用户 |
| 分片方式 | 按交易对 | 按用户（反向索引定位） |
| 核心数据结构 | 预计算 liq_price + 排序结构 | equity 缓存 + delta 估算 |
| 每次 tick 复杂度 | O(K)，K = 需强平数 | O(M)，M = 受影响用户数 |
| 跨资产影响 | 无 | 自然处理（equity 累积） |
| 突破后动作 | 直接强平 | 直接判断（无虚警） |

**这也是行业默认逐仓模式的原因之一** — 不只是用户端风险隔离，风控引擎的实现复杂度和性能也有本质差异。

---

#### 分层处理

无论哪种场景，Pre-trade 还需要"可用余额 / 保证金使用率"等聚合指标，不能只靠排序结构或 delta 估算。

```
第 1 层 — 强平判断（每次 tick）:
  分区一: 排序结构扫描 → 直接强平
  分区二: 反向索引 + delta 估算 → 直接判断

第 2 层 — 危险区用户完整状态更新（每次 tick，部分用户）:
  距强平线 < 5% → 每次 tick 更新完整风险画像

第 3 层 — 安全区用户状态更新（降频 / 按需）:
  距强平线远 → 每 N 次 tick 更新一次

Pre-trade 补算:
  安全区用户下单时，用最新 Mark Price 临时补算一次
```

---


### 状态更新的五个触发源

```
1. [Clearing Service]     成交结算完成 → 更新持仓和保证金
2. [Mark Price Service]   标记价格变动 → 重算未实现盈亏  ★ 触发频率最高
3. [Funding Service]      Funding 结算完成 → 更新可用余额
4. [Account Service]      充值/提现完成 → 更新可用余额
5. [运营后台]             风控参数调整 → 重算所有受影响用户的保证金率
```

其中 Mark Price 变动触发频率最高 — 每次价格更新都可能影响所有持仓用户的风险状态。这也是 Risk Service 性能压力最大的来源。

---

### 案例：Risk Service 风险状态计算积压

某交易所在价格剧烈波动时，Mark Price 更新过于频繁导致风险状态计算积压，强平信号延迟发出 — 等强平单终于发出时，用户已经穿仓。

**机制：**

1. BTC 价格在 5 分钟内剧烈波动 — Mark Price Service 高频推送价格更新
2. 每次价格更新 → Risk Service 需要重算所有 BTC 持仓用户的未实现盈亏和保证金率
3. 重算速度跟不上推送速度 → 计算队列积压
4. 已经触及强平线的用户，因为 Risk Service 还在处理更早的价格更新，强平信号延迟发出
5. 延迟期间价格继续恶化 → 等到强平单终于发出时，用户已经穿仓

**核心教训：** 更新速度必须跟得上市场变化速度。否则"实时"变成"延时"，风控名存实亡。

**回扣 2.2 架构 — 每一步瓶颈的解法：**

```
消息堆积         → latest-wins: 只取最新价格，跳过过期消息
重算所有用户太慢  → 分区一: liq_price + 排序结构 O(K)
                   分区二: delta 估算，每用户几次算术
多交易对单线程    → 分区一按交易对并行，分区二按用户并行
安全区用户占资源  → 分层更新: 安全区降频，Pre-trade 时补算

完整架构链路:
  消息层(latest-wins) → 分区层(仓位类型) → 分片层(交易对/用户)
  → 算法层(liq_price/delta) → 调度层(分层更新)
```

---

### 阶梯保证金制度（Tiered Margin）

保证金率不是一刀切，而是按仓位大小分档：

| 档位 | 名义价值范围 | 最大杠杆 | 维持保证金率 |
|------|-------------|---------|------------|
| 1 | < $50,000 | 125x | 0.40% |
| 2 | $50,000 - $250,000 | 100x | 0.50% |
| 3 | $250,000 - $1,000,000 | 50x | 1.00% |
| 4 | $1,000,000 - $5,000,000 | 20x | 2.50% |

仓位越大 → 强平时卖压越大 → 需要更高保证金率作为缓冲。阶梯表是运营预配置的（静态），但用户的实际保证金率随仓位大小自动切换档位（动态效果）。

```
跨档影响:
  user_A 加仓: $48,000(档1, 0.40%) → $52,000(档2, 0.50%)
  maintenance_margin: $192 → $260（增加 35%）
  → 强平价更近了 → 分区一重算 liq_price / 分区二重算 maint_margin

Pre-trade 必须用"成交后"的档位判断，不能用当前档位
  否则: 用 0.40% 判断"够" → 成交后跨入 0.50% → 保证金率瞬间不足 → 刚开仓就强平
```

---

## 五、风控的系统风险与架构位置总结

### 风控自身的两种过载

```
场景 1 — Pre-trade 过载:
  极端行情 → 用户疯狂下单 + Mark Price 高频变动
  → Risk Service 积压 → Pre-trade 判断延迟 → 用户下不了单 → 无法平仓止损

场景 2 — In-trade 连环清算（Death Spiral）:
  价格下跌 → 强平卖单 → 卖压压低价格 → 更多强平 → 更多卖单 → 正反馈循环
```

---

### 案例：2020 年 3.12 事件

BTC 单日暴跌约 50%，多个平台因风控/清算系统过载出现异常 — Mark Price 积压、强平延迟、穿仓面积扩大、Insurance Fund 快速消耗。

**核心教训：** 极端行情考验的不是某一个组件，而是"所有组件同时承压时，系统是否依然正常运转"。

**面对 3.12，正确的做法：**

价格是全市场决定的，单个交易所无法阻止价格下跌。交易所的职责是确保系统在极端压力下正常运行。

| 阶段 | 做什么 | 关键点 |
|------|--------|--------|
| **平时** | 系统容量按极端峰值设计（日常 10-50x） | BitMEX 3.12 宕机 = 系统容量不足 |
| | 持续积累 Insurance Fund | Fund 够大 → 穿仓吸收 → 不触发 ADL |
| | 阶梯保证金设计合理 | 大仓位高 MMR = 更厚安全垫 |
| | Mark Price 多源聚合鲁棒 | 极端行情下个别源异常 → 自动剔除 |
| **行情中** | 让自动机制正常运转（Mark Price → Risk → 强平 → Insurance Fund → ADL） | 不需要人工干预 |
| | 唯一人工干预：Reduce-Only（禁止开新仓，只允许平仓） | 新开仓 = 新增风险敞口 |
| **事后** | 补充 Insurance Fund + 系统故障补偿 + 复盘 | 补偿系统故障，不补偿市场亏损 |

**为什么不用熔断 / 暂停交易？**

- 加密 24/7 全球市场，你暂停了其他交易所还在交易 → 用户被锁仓更危险
- 恢复时积压订单集中涌入 → 二次崩盘
- "冻结强平"更危险：冻结不等于冻结亏损，解冻时穿仓规模翻倍

> 极端行情的应对 = 平时把 Mark Price + Insurance Fund + ADL 建好 + 系统容量按极端峰值设计 + 行情来了让自动机制运转 + 必要时 Reduce-Only。

---

### 风控架构位置图

```
用户 → API Gateway → Order Service
                          ↓
                    ┌─────────────┐
                    │ Risk Service│ ← Mark Price / Clearing / Funding 持续更新
                    │ (实时风险状态)│
                    └──┬──────┬───┘
        Pre-trade →    ↓      ↓    ← In-trade
              pass/reject    强平信号
                    ↓          ↓
            Matching Engine   Liquidation Engine
              ↓       ↑           ↑
        Clearing Service    (执行强平)
          ↓         ↓
   Account Service  Ledger Service
```

---

### 四个判断点

1. Risk Service 不是"Pre-trade 门卫"，而是持续运行的风险大脑 — 维护全局风险状态，同时服务 Pre-trade 和 In-trade
2. Risk Service 和 Liquidation Engine 的边界是"判断 vs 执行" — 决定"谁被强平" vs "怎么强平"
3. Pre-trade 必须同步阻塞，In-trade 触发依据必须是 Mark Price — 都是安全模型选择，不是性能权衡
4. 极端行情下应尽快清算而非人为延迟 — 加密市场极端行情几乎都是全市场联动，冷却期不会让外部市场停下来

---

### 核心命题回扣：为什么说"风控必须嵌在交易流程中"

1. **"嵌在流程中"** — Risk Service 是交易链路的同步环节，不是旁路系统
2. **两个输出方向** — Pre-trade 和 In-trade 基于同一份实时风险状态
3. **"判断 vs 执行"** — 只做判断，不执行资金操作 — 保证风控逻辑的纯粹性
4. **与第 1 节的关系** — Pre-trade 检查项来自 Account Service 余额字段
5. **与第 2 节的关系** — 强平单以市价单形式进入 Matching Engine，遵守价格优先·时间优先
6. **与第 3 节的关系** — 强平结算后写入 Ledger（biz_type: LIQUIDATION）

---

**下节预告：** 风控保护的是"系统内部的钱"。但钱怎么进出这个系统？→ 第 5 节：钱包与链上资产管理
