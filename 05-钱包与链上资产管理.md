# 第 5 节 | 钱包与链上资产管理

> **核心命题：** 钱包服务是交易所内部系统和链上世界的桥梁。链上的钱是"真的钱"（私钥控制），链下的是"账面上的钱"（Account Service 的数字）。这座桥的安全性和可用性，决定了交易所能不能"守住钱"。

---

## 本节要回答的问题

| 序号 | 问题 |
|------|------|
| 一 | 用户充值的币，从链上到"账户余额"经历了哪些步骤？什么时候算"到账"？ |
| 二 | 资金归集是什么？为什么需要？怎么优化成本？ |
| 三 | 冷钱包和热钱包为什么必须分开？比例怎么定？ |
| 四 | 用户提现的流程为什么这么复杂？每一步在防什么风险？ |
| 五 | 私钥管理为什么是交易所安全的"命门"？ |

---

## 一、充值：从链上交易到账户余额

**开场衔接：** 上节课 — "风控保护系统内部的钱"。这节课 — "钱怎么进出这个系统"。

---

### 充值地址的生成

**HD Wallet 派生（BIP-32/44）：**

```
Master Seed → Master Key → Purpose → Coin Type → Account

用户 A:
  ETH:   m/44'/60'/0'/0/0  → 0xabc...
  BTC:   m/44'/0'/0'/0/0   → 1ABC...
  Tron:  m/44'/195'/0'/0/0 → TABC...

用户 B:
  ETH:   m/44'/60'/0'/0/1  → 0xdef...
  BTC:   m/44'/0'/0'/0/1   → 1DEF...
```

- 一个 Master Seed 派生所有用户在所有链的地址，丢失时只需 Seed 即可恢复
- Wallet Service 批量预生成地址池，用户注册或首次充值时按需分配

**xpub/xprv 分离 — 地址生成与签名的安全隔离：**

```
Master Seed → xprv（锁在 HSM/MPC 中）
                ↓  hardened 派生（需要 xprv）
              xpub_EVM (覆盖所有 EVM 链) + xpub_BTC + xpub_Tron + ...
                ↓  普通派生（只需 xpub）
              用户 A 地址, 用户 B 地址, ...
```

- **xpub**：只能派生子公钥（地址），不能签名 → Wallet Service 只持有少量 xpub 即可生成所有充值地址
- **xprv**：能派生子私钥，能签名 → 锁在 HSM / MPC 中，仅签名时使用
- 即使 Wallet Service 被攻破 → 攻击者只拿到 xpub → 无法转走资产
- 这是后面"签名与广播分离"的安全基础

**多链地址策略：**

- EVM 兼容链（ETH/BSC/Arbitrum/Optimism/Polygon 等）：共用一个 xpub，同一用户跨链复用同一地址
- 非 EVM 链（BTC/Tron/Solana 等）：各自独立 xpub，地址不同
- 少数链（如 XRP）：共享地址 + Memo/Tag（链上创建账户有成本），用户忘填 Memo → 无法自动匹配 → 客服高频工单

> 归集签名时，MPC 各节点用 master share 派生子 share → 协作签名 → 完整私钥从未出现（详见第五节）。注意：上图"Master Seed → xprv"是概念模型；MPC 架构下，master shares 通过 DKG 协议直接生成，完整 xprv 从未存在（详见第五节）。

---

### 链上监听（Chain Scanner）

```
链上新区块/事件产生
  → Chain Scanner 获取区块数据
  → 逐笔检查：收款地址/memo 是否属于本所？
      • EVM 链: 原生币检查 tx.to，ERC-20 解析 Transfer 事件的 to 参数
      • UTXO 链 (BTC): 扫描交易输出（vout），检查脚本地址
  → 匹配成功 → 记录为"待确认充值"
  → 等待 N 个区块确认
```

**链上数据从哪来？**

| | 自建全节点 + 索引器 | 第三方 API |
|--|-------------------|-----------|
| 做法 | 节点拿原始区块数据 → 索引器建立地址→交易倒排索引，支持大规模查询 | 接入 Alchemy Webhooks 等，由平台推送转账事件 |
| 优点 | 完全可控，无外部依赖 | 接入快、运维成本低 |
| 缺点 | 运维负担重 | 依赖第三方可用性 |
| 适用 | 大所核心链 | 中小所 / 大所接入长尾链或新链快速上线 |

- 实际常见**混合模式**：核心链自建，长尾链接第三方，第三方同时作为对账源

**两个关键要求（不论哪种实现）：**

1. **不能漏扫** — 漏扫 = 用户充值丢失（游标管理 / Webhook 重试 + 定期对账）
2. **不能重复入账** — 重复入账 = 交易所凭空多给钱。幂等键：EVM 代币 `(txHash, logIndex)`、EVM 原生币 `(txHash)`、UTXO `(txid, vout)`

**案例：** 有交易所 Chain Scanner 在节点切换时跳过一个区块 → 该区块内充值全部未入账 → 大量客诉。

---

### 确认数策略 — 为什么不能一个确认就入账？

**区块重组（reorg）风险：** 交易被打包进区块，但后续出现更长分叉 → 原区块被废弃 → 交易"消失" → 如果已经入账 → 用户提走 → 交易所净损失。

交易所充值确认分两个阈值：**到账**（余额进入 Spot Account，可现货交易、理财）和**解锁**（可提现、转移、通过 P2P 出售）。解锁门槛更高 — 资金可能离开 Spot Account 甚至离开系统。

| 链 | 到账确认数 | 解锁确认数 | 说明 |
|----|------------|------------|------|
| BTC | 1 | 2 | PoW 最长链规则 |
| ETH (PoS) | 6 | 64 (~2 epochs) | 64 确认达到 PoS finality |
| BSC | 1 | 1 | PoSA 快速终局 |
| Tron | 1 | 1 | DPoS 快速终局 |
| Solana | 1 | 1 | 终局极快 |
| Arbitrum | Bundle 1 | Bundle 2 | Optimistic Rollup |
| Base | Bundle 1 | Bundle 2 | Optimistic Rollup |
| Optimism | Bundle 1 | Bundle 2 | Optimistic Rollup |

> 以 Binance 为参考，各交易所实际设置可能不同。

**L2 Bundle 确认：** L2 安全性依赖 L1 结算，用 Bundle（batch 提交 L1 后的确认）作为确认单位：

```
用户 L2 充值 → Sequencer 打包 → batch 提交 L1
  → Bundle 1 确认（batch 被 L1 接收并确认）→ 到账
  → Bundle 2 确认（L1 更深确认数达标）→ 解锁
```

- L2 不会等 Optimistic Rollup 的 7 天挑战期 — Bundle 在 L1 获得足够确认即可

**案例：** 2019 年 ETC 遭 51% 攻击，攻击者利用区块重组对交易所实施双花 → 事后多家交易所大幅提高 ETC 确认数。

**充值状态三段式：**

```
Pending（处理中）→ 到账确认达标 → Credited（Spot Account 内：现货交易、理财）→ 解锁确认达标 → Unlocked（可提现、转移、P2P 出售）
```

- 快速终局链（BSC/Tron/Solana）：到账 = 解锁 = 1 确认，三段式退化为 Pending → 直接可用
- 核心逻辑：到账后操作在 Spot Account 内（可冻结回滚），解锁后资金可提现、转移、P2P 出售，可能离开系统（不可逆）→ 解锁门槛必须更高
- Reorg 发生时：Pending → 直接作废；已入账 → 反向冲账 + 冻结排查（代价极高，确认数要留够余量）

---

### 入账流程

```
Chain Scanner 确认数达标
  → Wallet Service
  → Account Service: Spot Account available += 充值金额
  → Ledger Service: 写入充值账本 (biz_type: DEPOSIT)
```

- **幂等入账**：Chain Scanner 是 at-least-once，同一笔充值可能通知多次 → 入账前用幂等键查重（EVM 代币 `(txHash, logIndex)`、UTXO `(txid, vout)`），已存在则跳过
- Account Service 和 Ledger Service **必须同时成功**（第 3 节讲的"闭环"）
- 充值入账目标是 **Spot Account**（第 1 节）— 交易合约需先转到 Futures Account

**解锁状态如何实现？**

入账只在 Credited 阶段发生一次，Unlocked 阶段不动余额，只解除限制。两种思路：

- **思路 A：Account Service 加限制字段** — Spot Account 增加 `withdraw_locked` 字段。Credited 阶段：`available += amount`，`withdraw_locked += amount`；Unlocked 阶段：`withdraw_locked -= amount`。可提现/转移/P2P 额度 = `available - frozen - withdraw_locked`，现货交易和理财不受限。
- **思路 B：Wallet Service 充值记录驱动** — Account Service 不加字段，每笔充值记录在 Wallet Service 中有状态（`pending → credited → unlocked`）。提现/转移时动态计算：可操作额度 = `available - frozen - Σ(credited 但未 unlocked 的充值金额)`。

---

## 二、资金归集

### 为什么需要归集？

- 每个用户独立充值地址 → 币分散在成千上万个地址上
- 提现时不可能从每个用户地址分别转出
- **归集 = 把分散在用户充值地址上的资产，集中到热钱包（或热钱包余额充足时直接到 Warm Wallet）**

归集是纯链上操作，不影响 Account Service 余额（充值时已入账）。归集策略核心：降低攻击面 + 控制链上成本 + 优化热钱包余额 + 降低 UTXO / Gas 负担。不是"充值到了就立刻转走"。

---

### 归集策略的核心分类 — UTXO 模型 vs 账户模型

**按链的交易模型分策略，不是按链名分策略：**

| 模型 | 代表链 | 策略 | 归集方式 |
|------|--------|------|---------|
| UTXO | BTC, LTC | 批量归集 + UTXO 优化 | 阈值 + 定时批量，多输入合并一输出，UTXO Optimizer 控制碎片 |
| 账户模型 | EVM (L1+L2), Solana, Tron 等 | 阈值触发，逐地址归集 | 充值达阈值 → 自动归集到热钱包；每地址独立交易（无法批量） |
| 共享地址 | XRP 等 | 不需要归集 | 充值直接进入交易所地址（Memo 区分用户） |

**UTXO 批量归集 — 交易模型的天然优势：** 一笔 BTC 交易可以包含多个输入（不同用户的充值地址，各自派生私钥签名）→ 一个输出（Hot Wallet）。"聚合"发生在交易构造层面，不需要中间钱包。账户模型链做不到 — 每个地址必须各发一笔独立交易。

**代币归集的双重费用问题（EVM L1 最典型）：** 归集非原生代币（ERC-20 / SPL / TRC-20）→ 先往充值地址转原生币付手续费 → 再转出代币 = 两笔交易。EVM L1 Gas 高时成本突出；Solana / Tron / L2 费用极低，问题不大。原生币归集不需要先补费。

---

### 归集触发模型

| 模型 | 逻辑 | 适用 |
|------|------|------|
| 阈值触发 | `if balance > X → sweep` | 通用 |
| 时间批量 | 每 N 分钟扫描，批量 sweep | UTXO 链为主 |
| 风险触发 | 异常 / 安全事件 → 立即 sweep | 全链通用 |

极端行情下动态调整：暴跌→加快归集确保出金；链拥堵→暂停 EVM 归集；安全事件→立即归集。

---

### 归集路径 — 动态路由

```
Deposit Address
      ↓  Sweep Service（检查热钱包水位）
      ├─→ Hot Wallet    ← 常态路径（热钱包需要补充提现消耗）
      └─→ Warm Wallet   ← 溢出路径（热钱包余额 > 高水位）
              ↓
         Cold Wallet（低频，长期存储）
```

- 常态下大部分归集进 Hot — 热钱包持续被提现消耗，需要补充
- 热钱包余额充足时，溢出部分路由到 Warm
- UTXO 链额外优势：一笔交易可同时输出到 Hot 和 Warm，交易层面完成动态分配

---

### 为什么不能"充值即刻 sweep"？

| 原因 | 说明 |
|------|------|
| 成本太高 | EVM 双重 Gas，UTXO 小额手续费 |
| UTXO 爆炸 | BTC 大量小 UTXO → 未来提现成本爆炸 |
| 签名系统压力 | 大量并发请求冲击 Signing Service |
| 链上轨迹可分析 | 即刻归集形成可识别的资金聚合模式 |

---

### 工程实现

归集是**独立微服务（Sweep Service）**：Sweep Queue（按链分区）+ Rate Limiter + Gas Optimizer（EVM）+ UTXO Optimizer（BTC）+ 动态路由（Hot/Warm）+ 多签批处理。

**案例：** 有交易所归集阈值设太低 → Gas 费远超归集金额 → 引入 Gas 价格监控 + 提高阈值后月 Gas 费大幅下降。

> **判断点：** 归集策略是"分链定制 + 批量触发 + 动态路由 + 动态风险控制"，不是简单的"充值到了就转走"。

---

## 三、冷热钱包架构

### 为什么需要分？

归集常态路径直接进热钱包（补充提现消耗），溢出时路由到 Warm Wallet。**热钱包是出金枢纽**，必须在线，但**在线 = 暴露**。冷热分离的本质：只让一小部分资产承担在线风险，大部分离线隔离。

---

### 热钱包（Hot Wallet）

用户提现的出金来源 — 接收到提现请求后，热钱包签名并广播链上交易。通过 Signing Service 自动签名，私钥在线（HSM / MPC）。被攻破 = 该钱包余额立即丢失。

**每条链可以有多个热钱包：**

```
ETH: HW-A (addr 1), HW-B (addr 2)  — 并发处理提现
BSC: HW-C (addr 3)
```

| 目的 | 说明 |
|------|------|
| 吞吐量 | 多钱包并发处理提现，提高速度 |
| 风险隔离 | 单个热钱包被盗，损失限制在该钱包余额内 |
| 运维灵活 | 可轮换、下线单个钱包而不中断服务 |

余额来源：归集直接进入（常态）+ Warm → Hot 调拨（补充）。余额过多时：热转冷（降低在线风险）。

---

### 冷钱包（Cold Wallet）

存放大部分资产（85%+），三层隔离：

| 隔离层 | 做法 | 防什么 |
|-------|------|-------|
| 私钥离线 | 存储在离线硬件钱包（如 Ledger），从不接触互联网 | 黑客无法远程盗取 |
| 多签 | 冷钱包地址为多签（如 3-of-5），需多把私钥同时签名 | 单个私钥泄露不会丢币 |
| 地理分离 | 各签名私钥分布在不同物理位置 | 攻击者必须同时攻破多个地点 |

**气隙签名流程：**

```
在线机器构造交易 → QR/USB 传到离线设备 → 人工验证交易内容 → 离线签名
    → QR/USB 传回在线机器 → 收集足够多签签名 → 广播上链
```

私钥全程不触网 — "气隙"指的就是在线与离线之间没有任何网络连接，数据传输只通过物理介质（QR 码 / USB）。

---

### 温钱包（Warm Wallet）— 为什么不能只有冷 + 热？

**只有冷 + 热的问题：** 热钱包耗尽 → 需要从冷钱包补充 → 冷钱包离线、多人到场、数小时流程 → 提现高峰时用户几小时提不了现。你要么在热钱包放更多（风险高），要么每次都走冷钱包全流程（太慢）。

**温钱包 = 热钱包的快速储备池。** 在线但不对外暴露，审批后自动签名，分钟级补充热钱包。

| | Hot Wallet | Warm Wallet | Cold Wallet |
|--|-----------|-------------|-------------|
| 私钥状态 | 在线，自动签名 | 在线，但需审批触发 | 离线，人工签名 |
| 用途 | 用户提现 | 热钱包储备（不对外暴露） | 长期存储 |
| 补充热钱包 | — | 分钟级（审批后自动签名） | 小时级（多人到场） |
| 安全风险 | 高 — 直接连接提现 API | 中 — 在线但不对外，需突破审批 | 低 — 无法远程攻击 |
| 资产占比 | 1–5% | 5–10% | 85%+ |

并非所有交易所都叫"温钱包" — 但只要规模到了一定程度，都需要在"全自动出金"和"全人工冷存储"之间有一个缓冲层。

---

### 冷热调拨

```
热转冷（常规，风险低）：热钱包余额 > 高水位 → 审批 → 热钱包签名转出 → 冷钱包
冷转热（敏感，风险高）：热钱包余额 < 低水位 → 多人审批 → 冷钱包多签签名 → 热钱包
```

**为什么冷转热风险最高？**

| 因素 | 说明 |
|------|------|
| 时间紧迫 | 通常发生在提现激增时（市场暴跌 → 恐慌提现），没有从容操作的余裕 |
| 人工操作 | 气隙签名全程人工 — 转错地址、金额错误、签错交易都可能发生 |
| 多签协调 | 多个签名持有人可能不在同一时区，紧急时协调困难 |
| 攻击窗口 | 紧急 + 人工 = 最容易被社会工程 / 供应链攻击利用的环节 |

Bybit 事件（2025.02，约 15 亿美元 ETH）正是在冷转热的多签签名环节被攻击 — 签名界面被篡改，多签持有人以为自己在签正确的交易，实际签的是攻击者构造的交易。

**预案：** 提前定义调拨金额梯度和审批人 + 定期演练冷转热流程（不要等到真正紧急时才第一次操作）+ 签名界面独立验证，不依赖单一前端（Bybit 教训）

**水位监控：** 热钱包余额 ≥ 日均提现量 × 1–3 天，按链 × 按币种 × 按地址监控

---

### 历史案例

| 事件 | 时间 | 损失 | 根因 | 哪层防线失败 |
|------|------|------|------|------------|
| Mt. Gox | 2014 | 约 85 万 BTC | 私钥长期被盗 | 热钱包无监控无隔离 |
| Bitfinex | 2016 | 约 12 万 BTC | 多签方案被突破 | 多签本身有漏洞 |
| Coincheck | 2018 | 约 5.3 亿美元 NEM | 全部热钱包 | 没有冷热分离 |
| KuCoin | 2020 | 约 2.8 亿美元 | 热钱包私钥泄露 | 私钥保护不足 |
| Bybit | 2025.02 | 约 15 亿美元 ETH | 多签签名界面被篡改 | 签名环节的供应链 |

**防线演进：** 第一代（无冷热分离）→ 第二代（冷热分离但热钱包/多签被破）→ 第三代（全部到位但签名前端被篡改）— 每代解决上代问题，又暴露新攻击面。

> **判断点：** 冷热架构不只是"比例分配"，核心是理解每一层防线的攻击面在哪里。

> **判断点：** 冷热架构的本质是"用可用性换安全性" — 即使热钱包被完全攻破，大部分资产仍然安全。

---

## 四、提现：从余额到链上转账

> 提现比充值复杂得多 — 因为提现是"钱流出系统"，每一步都有被攻击或出错的可能。

---

### 提现完整链路

```
用户提交提现请求（提 3000 USDT 到 0xabc...）
  → Withdraw Service → Account Service:
      ① 校验 withdrawable = available - withdraw_locked ≥ 3000
      ② 冻结: available -=3000, frozen +=3000
  → Risk Service 风控审核:
      ☑ 提现地址白名单
      ☑ 单笔/单日限额
      ☑ 行为异常检测
      ☑ AML / Chain analysis
      ☑ 大额人工审核
  → 审核通过
  → Wallet Service 构造链上交易
  → Signing Service 签名（HSM / MPC）
  → Wallet Service 广播到链上
  → Chain Scanner 监控确认
```

**成功路径：** 确认 → 扣除 frozen → 写入 Ledger（biz_type: WITHDRAW）

**失败路径：** 失败/超时 → 解冻: frozen -=3000, available +=3000 → 重试或告警

**为什么先冻结再审核？** 不冻结 → 审核期间用户可能用掉这笔钱 → 审核通过时余额不够 → 要么提现失败，要么超额提现。

---

### 签名与广播的分离

```
Wallet Service    构造待签名交易（to, value, nonce, gas）
      ↓
Signing Service   独立安全环境，用私钥签名
      │           • 只接收待签名数据，返回签名结果
      │           • 不处理业务逻辑、不直接联网
      │           • 不知道这笔交易是提现还是归集
      ↓
Wallet Service    接收签名后的交易 → 广播到链上
      ↓
链上              交易进入 mempool → 打包进区块
```

> **判断点：** 签名服务 = 系统中权限最小、访问最严格的组件。私钥不离开安全环境，Wallet Service 永远接触不到私钥。

---

### 提现失败场景

| 失败类型 | 原因 | 处理 |
|---------|------|------|
| Gas 不足 | 热钱包原生币不够 | 补充 Gas → 重试 |
| Nonce 卡住 (EVM) | 某笔交易未确认，后续全部排队 | 用相同 Nonce 重发（加速/取消） |
| 链拥堵 | Gas 价格飙升 | 提高 Gas 重发 |
| 目标地址异常 | 合约不接受 / 格式错误 | 解冻 → 通知用户 |

**提现队列架构与 Nonce 管理（EVM 链）：**

EVM 链的 Nonce 规则：每个地址的交易必须按 Nonce **严格递增**（0, 1, 2, 3...），Nonce 3 未确认 → Nonce 4 永远不会被打包。这意味着**同一个热钱包地址的提现交易必须有序处理** — 队列（FIFO）是保证有序的天然选择。在此基础上，通过按链分区 + 多热钱包并发，解决吞吐量问题：

```
┌──────────────────────────────────────────────────────────────────────┐
│                    提现队列架构                                        │
│                                                                      │
│  生产者: Withdraw Service                                            │
│  - 用户提交提现 → 校验 withdrawable → 冻结 → 风控审核通过              │
│  - 按链投递到对应队列（不需要知道哪个热钱包处理）                       │
│                                                                      │
│  消费者: Wallet Service（管理热钱包）                                  │
│  - 从队列取出请求 → 构造交易 → 调 Signing Service 签名 → 广播上链     │
│  - 队列是 Withdraw Service 和 Wallet Service 的解耦点                 │
│    两边互不感知对方的实例数和内部逻辑                                   │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────┐         │
│  │ Withdraw Service                                        │         │
│  │   审核通过 → 按链投递                                    │         │
│  └──┬──────────┬──────────┬────────────────────────────────┘         │
│     ↓          ↓          ↓                                          │
│  ETH Queue  BSC Queue  BTC Queue       ← 按链分区                   │
│   ↑    ↑      ↑           ↑                                         │
│   │    │      │           │             ← Wallet Service 消费        │
│  HW-A HW-B  HW-A        HW-C                                       │
│  └─────────────────────────────────────────────────────────┘         │
│                                                                      │
│  两层并行：                                                           │
│  ① 跨链并行 — Wallet Service A 同时消费 ETH 和 BSC 队列              │
│    不同链 Nonce 序列完全独立，互不阻塞                                 │
│  ② 同链并发 — ETH Queue 有多个热钱包（A、B）竞争消费                  │
│    各自用各自的地址和 Nonce，天然并行不冲突                             │
│                                                                      │
│  Nonce 管理：                                                        │
│  - 每个热钱包有自己的地址、自己的 Nonce 序列                           │
│  - 从队列取出请求 → 用自己的 Nonce 递增签名 → 不存在跨钱包冲突         │
│  - 不需要"全局 Nonce Manager" — 队列 + 多热钱包架构把问题消解了        │
│                                                                      │
│  剩余挑战 — 单个热钱包内的卡住处理（head-of-line blocking）：          │
│  - 某笔交易 Gas 不足或链拥堵 → 迟迟未确认 → 后续提现全部排队          │
│  - 处理方式: 超时监控 → 用相同 Nonce 重发（提高 Gas 加速，或取消）     │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 五、私钥管理 — 交易所安全的"命门"

### 核心问题

**"谁能动这笔钱" = "谁持有私钥"。** 链上没有冻结、回滚、挂失。

核心问题：**如何确保没有任何单一个体能够独自动用资产？**

---

### 三种方案

| | HSM | MPC | Multi-sig |
|--|-----|-----|-----------|
| **全称** | Hardware Security Module | Multi-Party Computation | Multi-Signature |
| **原理** | 私钥存储在专用硬件中，签名在硬件内完成，私钥不可导出 | 私钥拆分成多个碎片（shares），各方协作完成签名，任何单方不持有完整私钥 | 链上合约/协议要求 M-of-N 个签名才能执行交易 |
| **私钥暴露风险** | 低 — 私钥不离开硬件 | 低 — 完整私钥从未在任何地方出现 | 低 — 单一签名不足以转移资产 |
| **单点风险** | 硬件损坏 / 被物理盗取 | 较低（需攻破多方） | 较低（需获取多个密钥） |
| **灵活性** | 中 — 硬件更换/扩容受限 | 高 — 可调整参与方、支持跨链 | 受链支持限制 |
| **链支持** | 通用（在签名层面工作） | 通用（在签名层面工作） | 需要链原生支持或智能合约实现 |
| **代表实现** | Thales、Utimaco | Fireblocks、ZenGo | Safe{Wallet}（原 Gnosis Safe）、BTC P2SH |

**签名过程对比：**

```
HSM:
  Wallet Service → 发送待签名数据 → HSM 硬件内部签名 → 返回签名结果
  （私钥不离开硬件）

MPC (2-of-3):
  节点 A (share_1) ─┐
  节点 B (share_2) ─┼→ MPC 协议交互 → 生成完整签名
  节点 C (share_3) ─┘  （任意 2 方参与即可，完整私钥从未出现）

Multi-sig (2-of-3):
  签名者 A → sig_A ─┐
  签名者 B → sig_B ─┼→ 链上合约验证 ≥ 2 个有效签名 → 执行交易
  签名者 C → sig_C ─┘
```

**关键区别：**
- HSM：私钥完整存在，但被封装在硬件内
- MPC：私钥从未以完整形式存在于任何地方
- Multi-sig：每个签名者持有各自完整私钥，链上验证多签

---

### 为什么单一方案不够？

| 方案 | 防住了什么 | 没防住什么 |
|------|-----------|-----------|
| HSM | 私钥不可提取 | 不防恶意签名请求 — Wallet Service 被入侵后可合法调用 HSM |
| MPC | 单点无法获取完整私钥 | 不防前端/通信层篡改 — 各方看到的签名内容可能被篡改 |
| Multi-sig | 单人无法动资产 | 不防所有签名者被同时欺骗（Bybit 事件） |

→ 实践中必须组合使用，没有任何一种方案能单独覆盖所有攻击面。

---

### 充值地址的私钥 — 百万地址怎么安全签名？

交易所有数百万个充值地址（第一节 HD Wallet 派生），归集时需要用每个地址的派生私钥签名。这个场景有三个特殊要求：**HD 派生（百万地址）+ 全自动化（高频归集）+ 无单点泄露**。

**为什么 HSM 和 Multi-sig 不适合？**

| 方案 | 问题 |
|------|------|
| 传统 HSM | 不原生支持 BIP-32 派生；完整 xprv 在单台硬件中 — 被攻破 = 所有地址泄露；硬件单点故障 |
| Multi-sig | 每个充值地址须部署为多签地址（百万级不现实）；归集是全自动高频操作 — 多签需人工协调，无法自动化 |

→ **MPC 是唯一同时满足三个要求的方案：HD 派生 + 阈值安全 + 可全自动化。**

**master shares 从哪来？— DKG（分布式密钥生成）：**

- 第一节"Master Seed → xprv"是 HD Wallet 概念模型。如果先生成完整 xprv 再拆 shares — 生成那一刻完整私钥就存在过了
- MPC 用 DKG 协议：各节点各自生成随机数 → 密码学协议交换 → 每个节点得到自己的 master share
- 这些 shares 数学上定义了一个 master private key，但这个 key 从未被计算出来
- 各节点协作推导出 xpub → Wallet Service 用 xpub 生成充值地址
- **不存在的东西无法泄露 — 这才是根本性安全**

**MPC 节点的部署要求 — DKG 安全的前提是节点本身安全：**

| 要求 | 说明 |
|------|------|
| 物理与逻辑隔离 | 各节点在不同服务器 / 网段 / 云厂商；同一台机器 = MPC 形同虚设 |
| 权限分离（防串谋） | 核心是持有不同 shares 的团队之间没有共同行动便利。纯自托管（大所主流）：不同团队 + 不同汇报线 + 不同物理位置；或交易所 + MPC 服务商（如 Fireblocks）：多一层外部制衡，但多一个信任点 |
| 阈值设计 | 2-of-3：容忍 1 节点宕机，攻击者需攻破 2 个；3-of-5：容忍 2 个，需攻破 3 个。热钱包用低阈值（可用性优先），温钱包用高阈值（安全优先） |
| Share 备份 | share 永久丢失 + 达不到阈值 = 资产永久锁死；每个节点独立备份，备份本身也需隔离 |
| 主动份额刷新 | 定期生成新 shares（对应同一虚拟私钥），旧 shares 作废；攻击者偷到旧 share → 刷新后失效 → 限制攻击窗口 |

**具体签名流程 — MPC-TSS 原生支持 BIP-32 HD 派生，不需要单独存储任何一个子私钥：**

```
Sweep Service → "为地址 m/44'/60'/0'/0/0 签名"
    → Signing Service → Policy Engine 检查通过
    → MPC 各节点：用各自 share 按路径派生子 share → 协作签名
    → 返回签名结果（完整私钥从未在任何地方出现）
```

- 只存 master shares — 可以派生所有充值地址的子 shares
- MPC 协议原生支持 BIP-32 派生（不依赖特定硬件）
- 完整私钥（master 或 child）从未以完整形式出现在任何地方
- 各节点的 share 存储在 HSM 中 — HSM 保护 share 不可提取
- UTXO 批量归集：一笔交易多个输入 → MPC 为每个输入分别派生子 shares 并协作签名

**HSM 在 MPC 架构下的角色：** 不是直接管 xprv — 而是保护各 MPC 节点的 share 不可提取，即使节点被入侵也无法导出。

---

### 分层密钥策略

不同钱包层用不同方案组合：

| 钱包层 | 方案 | 原因 |
|--------|------|------|
| Hot Wallet | MPC + HSM 保护 share | 需要自动化（秒级响应）；MPC 确保没有单点，原生支持 HD 派生；HSM 保护各节点 share 不可提取 |
| Warm Wallet | MPC（更高阈值） | 需在线但有审批；阈值更高（如 3-of-5），攻击者需攻破更多节点 |
| Cold Wallet | Multi-sig + 硬件钱包（离线） | 全人工、追求最高安全性；Multi-sig 提供链上可验证性；各签名者用硬件钱包（如 Ledger）— 设备屏幕独立验证交易内容，不依赖可能被篡改的电脑界面 |

冷钱包用 Multi-sig 而非 MPC：Multi-sig 验证逻辑在链上，任何人可验证签名是否合规；MPC 签名在链上看和普通签名一样，无法区分是否经过多方协作。

---

### Signing Service — 签名策略引擎

私钥保管只是一半，另一半是**谁能发起签名、什么请求能通过**：

```
Wallet Service → 签名请求 → Policy Engine（策略引擎）→ 通过 → HSM / MPC 签名
                                                  → 拒绝 → 告警
```

| 策略 | 检查内容 |
|------|---------|
| 白名单 | 目标地址是否在允许列表？ |
| 限额 | 单笔 / 日累计 / 小时累计是否超限？ |
| 频率 | 短时间内请求数量是否异常？ |
| 审批梯度 | 按金额决定：自动通过 / 1 人审批 / 多人审批 |

Policy Engine 是最后一道防线 — 即使 Wallet Service 被入侵，不在白名单的地址、超限的金额、异常的频率 → 全部拒签。每笔签名请求（无论成败）都写入审计日志。

---

### 密钥生命周期

| 阶段 | 关键操作 | 风险点 |
|------|---------|--------|
| 生成 | 在安全环境（HSM / TEE）内，确保真随机 | 生成环境被污染 → 可预测私钥 |
| 存储 | 按层级：热钱包在线 HSM、冷钱包离线硬件、MPC shares 分布式 | 备份不当 → 丢失或泄露 |
| 使用 | 通过 Signing Service，受 Policy Engine 管控 | 签名请求伪造 / 策略绕过 |
| 轮换 | 定期更换（尤其人员变动时），资产迁移到新地址 | 轮换窗口期新旧密钥并存 |
| 恢复 | Shamir Secret Sharing / 多地备份 | 恢复流程本身可能成为攻击入口 |

人员变动是密钥轮换最常见的触发器：持有 MPC share 或 Multi-sig 私钥的员工离职 → 必须立即轮换 → 链上资产迁移到新地址。

---

### Bybit 事件的教训

**Bybit（2025.02）** 使用 Safe{Wallet} 多签管理冷钱包 → 攻击者通过供应链攻击篡改签名界面 → 签名者看到的是正常交易，实际提交的是恶意交易 → 多签全部通过 → 约 15 亿美元 ETH 被盗。

**教训：** 多签保证"多人同意才能动钱"，但如果签名者看到的交易内容是假的 — 多签形同虚设。安全不仅是"多少人签"，更是"如何独立验证交易内容"。

> 以上案例基于公开报道，供教学讨论。

---

### 四个判断点

1. 充值和提现不是"转账"那么简单 — 链上确认、安全审核、失败处理，任何一步出错都可能导致资金丢失或被盗
2. 冷热钱包的分离是"用可用性换安全性" — 比例取决于日常业务量和风险承受能力
3. 归集是资金管理的基础操作 — 不归集则提现困难，频繁归集则成本高
4. 私钥管理是交易所安全的"命门" — 方案本身不是终点，验证流程和供应链安全同样关键

---

### 核心命题回扣：为什么说"钱包是桥梁"

1. **充值 = 链上 → 链下**：链上交易经过监听、确认、入账 → Account Service 的数字
2. **归集 = 桥的内部物流**：分散资产集中到热钱包（常态）或 Warm（溢出）
3. **冷热钱包 = 桥的金库**：大部分离线，小部分在线
4. **提现 = 链下 → 链上**：Account Service 的数字经过冻结、审核、签名、广播 → 链上交易
5. **私钥 = 桥的钥匙**：控制私钥 = 控制链上资产
6. **与第 1 节的关系**：充值入 Spot Account，提现从 Spot Account 扣
7. **与第 3 节的关系**：每笔充值/提现都写入 Ledger
8. **与第 4 节的关系**：提现风控 = 现货风控的核心场景

---

**下节预告：** CEX 五大系统讲完。接下来进入 DEX — 信任模型从"相信交易所"变成"相信代码"，钱包不再由交易所管理。→ 第 6 节：Orderbook DEX 架构与核心判断。
