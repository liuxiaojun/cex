# 第 2 节 | 撮合引擎为什么必须是系统中心

> **核心命题：** 撮合不是一个"功能模块"，它是并发场景下的状态裁决中心。

---

## 本节要回答的问题

| 序号 | 问题 |
|------|------|
| 一 | 撮合引擎到底在解决什么问题？为什么说它是"裁决"而不是"匹配"？ |
| 二 | Orderbook 的内部结构是什么样的？一笔订单进来后怎么逐级匹配？ |
| 三 | 不同订单类型（Limit / Market / IOC / FOK / Post-Only）对撮合行为有什么约束？ |
| 四 | 为什么撮合不能拆成多个实例？性能瓶颈怎么解决？ |
| 五 | 撮合引擎的职责边界在哪里？它不做什么，为什么不做？ |

---

## 一、撮合引擎到底在解决什么问题

**开场衔接：** 上节课 — "钱怎么管"。这节课 — "谁来裁决成交"。

---

### 表面 vs 实际

- 表面上是"价格匹配" — 买单和卖单配对
- 实际上是"并发状态裁决" — 多笔订单争夺同一个价格位，谁先成交？
- 类比：法官，不是计算器

**核心属性：确定性** — 对同一组输入，同一时刻只能有一个结果。

---

### 并发裁决：为什么需要单一定序点

```
3 笔买单同时到达，争抢同一笔卖单（SELL 0.5 BTC）:
  Order-A: BUY 0.3 BTC    总需求 0.9 BTC > 供给 0.5 BTC
  Order-B: BUY 0.4 BTC
  Order-C: BUY 0.2 BTC

没有定序 → A 先到和 B 先到的结果完全不同
有定序   → 结果唯一、可重放、可审计

关键：定序 ≠ 同时处理。撮合是逐笔处理的，不存在"同时"。
```

> 如果有两个组件可以同时做出成交裁决，一定会出现矛盾结果。

---

### Price-Time Priority

1. **价格优先**：出价更高的买单先成交，出价更低的卖单先成交
2. **时间优先**：同价位下，先到的订单先成交

行业标准规则。规则简单透明，激励报最优价格，收窄 spread。

---

### 撮合不关心订单来源

```
Order Service ──┬── Account Service(冻结)
                └── Risk Service(Pre-trade 检查)
                      │
                 都通过 → Order Service 发到 MQ → ★ Matching Engine → Clearing / Market Data

到达撮合的订单已通过资金冻结和风控 — 撮合无需重复判断
```

---

## 二、Orderbook 的数据结构与撮合流程

### Orderbook 结构

![Image](https://mmbiz.qpic.cn/mmbiz_png/Xibk1Sk7nmickMuZojQljMEnSUWM7qNVUDfDyaTqQ8lcIP0cOZWVZSQXw9eWbRe7yicAlJUSbXhvicGXLNprPfBr8Q/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)

```
Orderbook (BTC/USDT):

── Asks（卖单，价格从低到高）──
价格 10020 | [Order-F: 0.5 BTC, Order-G: 1.2 BTC]    ← best ask
价格 10050 | [Order-H: 2.0 BTC]
价格 10100 | [Order-I: 0.8 BTC]

── Bids（买单，价格从高到低）──
价格 10000 | [Order-A: 1.0 BTC, Order-B: 0.3 BTC]    ← best bid
价格  9980 | [Order-C: 2.0 BTC]
价格  9950 | [Order-D: 0.5 BTC, Order-E: 1.0 BTC]

Spread = best ask - best bid = 10020 - 10000 = 20 USDT
```

- 买方（Bids）：价格从高到低，同价按时间
- 卖方（Asks）：价格从低到高，同价按时间
- 每个价格档位内部是 FIFO 队列

**数据结构：** 红黑树/跳表（按价格索引）+ 每个价格档位内的 FIFO 队列（按时间排序）

```
操作复杂度:
  查询 best bid/ask:   O(1)     ← 缓存头节点
  插入/删除价格档位:    O(log N) ← 红黑树/跳表
  同价位内插入/删除:    O(1)     ← 链表操作
  N = 价格档位数量
```

---

### 撮合循环流程

```
┌─────────────────┐
│  新订单进入       │
└────────┬────────┘
         │
         ▼
┌─────────────────────────┐
│  remaining > 0 ?        │
└────┬────────────────┬───┘
     │ 是              │ 否
     ▼                 ▼
┌─────────────────┐  ┌──────────────┐
│ 获取对手方       │  │ 撮合结束      │
│ best_opposite   │  │ (全部成交)    │
└────────┬────────┘  └──────────────┘
         │
         ▼
┌─────────────────────────┐
│  best 存在 且 价格匹配?  │
└────┬────────────────┬───┘
     │ 是              │ 否
     ▼                 ▼
┌─────────────────┐  ┌──────────────────────┐
│ 成交             │  │ 剩余部分挂入           │
│ 成交量 = 两者取小 │  │ Orderbook 等待匹配     │
│ 成交价 = 被动方   │  └──────────────────────┘
│ (Maker 定价)    │
└────────┬────────┘
         │
         ├──▶ 输出 Trade 事件
         │
         ▼
┌─────────────────────────┐
│  best 完全成交?          │
│  → 是: 从 Orderbook 移除 │
│  → 否: 更新 remaining    │
└────────┬────────────────┘
         │
         └──▶ 回到 "remaining > 0?" 继续循环
```

---

### 逐步匹配示例

```
当前 Orderbook:
    best ask = 10020 (Order-F: 0.5 BTC, Order-G: 1.2 BTC)
    best bid = 10000 (Order-A: 1.0 BTC)

新订单进入: BUY 1.2 BTC @ 10050 (限价买单)

第 1 轮: 与 best ask 匹配
    → 价格匹配? 10050 >= 10020 ✓
    → 成交: 0.5 BTC @ 10020 (Maker 定价)
    → Order-F 完全成交，从 asks 移除
    → 新订单剩余: 0.7 BTC

第 2 轮: 新的 best ask = 10020 (Order-G: 1.2 BTC)
    → 价格匹配? 10050 >= 10020 ✓
    → 成交: 0.7 BTC @ 10020
    → Order-G 剩余 0.5 BTC
    → 新订单剩余: 0 BTC → 完全成交

结果: 产生 2 笔 Trade，新订单以两个价格分别成交
```

> 一笔订单可能产生多笔成交 — "平均成交价"和"挂单价"往往不同

---

### Maker 定价规则

```
Maker 先到，出价是"已公示的承诺"
Taker 后到，出价是"我愿意的上限"
→ 用 Maker 价格成交 = Taker 获得价格改善

Spread = best ask - best bid = Maker 之间的"共识差距"
→ Spread 越小 = 流动性越好
→ Taker 每次成交都在"跨越 spread" — spread 是主动交易的成本
```

---

### 大单"吃穿"滑点演算

```
Asks:
  10,020 | 0.5 BTC
  10,025 | 1.0 BTC
  10,030 | 1.5 BTC
  10,050 | 3.0 BTC
  10,100 | 4.0 BTC

BUY 10 BTC @ Market:
  第 1 档: 0.5 BTC @ 10,020 = 5,010
  第 2 档: 1.0 BTC @ 10,025 = 10,025
  第 3 档: 1.5 BTC @ 10,030 = 15,045
  第 4 档: 3.0 BTC @ 10,050 = 30,150
  第 5 档: 4.0 BTC @ 10,100 = 40,400
  ───────────────────────────
  总计 10 BTC = 100,630 USDT
  平均成交价 = 10,063
  vs best ask 10,020 → 滑点 0.43%
```

> 单量越大 → 吃掉的档位越多 → 滑点越大

---

## 三、不同订单类型的撮合行为

### 五种订单类型的行为差异

**Limit Order（限价单）**

- 指定价格和数量
- 能匹配就匹配，剩余挂单等待
- 是 Orderbook 的流动性供给方

```
  ┌─── 能完全匹配 ───┐     ┌─── 部分匹配 ───────────┐     ┌─── 无法匹配 ─────┐
  │  全部成交        │     │  匹配部分: 立即成交      │    │  整笔挂入          │
  │  订单完结        │     │  剩余部分: 挂入 Orderbook│   │  Orderbook 等待    │
  └────────────────┘     └─────────────────────────┘   └───────────────────┘
```

**Market Order（市价单）**
- 只指定数量，不指定价格
- 从对手方最优价逐档吃，直到数量满足
- 不会挂在 Orderbook 上 — 剩余取消
- 风险：大额市价单"吃穿"深度，造成巨大滑点

```
  ┌─── 能完全匹配 ───┐     ┌─── 部分匹配 ─────────────┐     ┌─── 无法匹配 ─────┐
  │  全部成交        │     │  匹配部分: 立即成交       │     │  整笔取消 ❌      │
  │  订单完结        │     │  剩余部分: 直接取消 ❌    │     │  不挂单            │
  └────────────────┘     └─────────────────────────┘     └───────────────────┘
```

> 案例：2019 年 Bitstamp 约 5,000 BTC 大额市价卖单，吃穿买方深度 → BTC 短时下跌约 6% → 联动触发 BitMEX 等平台约 $2 亿连锁强平

**IOC（Immediate or Cancel）**

- 立即匹配能成交的部分，剩余取消
- vs Market: IOC 可以指定价格上限/下限

```
  ┌─── 能完全匹配 ───┐     ┌─── 部分匹配 ─────────────┐     ┌─── 无法匹配 ─────┐
  │  全部成交        │     │  匹配部分: 立即成交       │     │  整笔取消 ❌      │
  │  订单完结        │     │  剩余部分: 直接取消 ❌    │     │  不挂单            │
  └────────────────┘     └─────────────────────────┘     └───────────────────┘
```

**FOK（Fill or Kill）**
- 全部成交或全部取消 — 没有部分成交
- 撮合引擎需先预判能否全量匹配

```
  ┌── 先预判：能否全量匹配？───────┐
  │                             │
  ┌─── 能完全匹配 ───┐     ┌─── 不能完全匹配 ──┐
  │  全部成交        │     │  整笔取消 ❌       │
  │  订单完结        │     │  Orderbook 不变     │
  └─────────────── ─┘     └────────────────────┘
```

**Post-Only（只做 Maker）**
- 如果会立即成交（成为 Taker），则拒绝
- 确保享受 Maker 手续费优惠
- 高并发下可能判断不准 — 宁可拒绝也不让 Post-Only 变成 Taker

```
  ┌── 先判断：会不会立即匹配？───────┐
  │                              │
  ┌─── 会匹配（成为 Taker）──┐  ┌─── 不会匹配 ──────────┐
  │  整笔拒绝 ❌            │  │  挂入 Orderbook        │
  │  Orderbook 不变         │  │  确保为 Maker           │
  └────────────────────────┘  └────────────────────────┘
```

---

### 订单类型对比表

| 订单类型 | 是否指定价格 | 未成交部分 | 是否提供流动性 | 撮合引擎额外逻辑 |
|---------|------------|----------|-------------|----------------|
| Limit | 是 | 挂单等待 | 是（Maker） | 无 |
| Market | 否 | 取消 | 否（Taker） | 保护价机制 |
| IOC | 是 | 取消 | 否 | 无 |
| FOK | 是 | 全部取消 | 否 | 预判能否全量匹配 |
| Post-Only | 是 | 挂单 | 是（强制 Maker） | 预判是否会立即成交 |

> 订单类型越多，分支逻辑越复杂。但每种类型的行为必须**完全确定性**，不能有模糊空间。

---

### 市价单保护价机制

```
基准: best ask = 10,020
保护价 = 10,020 × (1 + 5%) = 10,521
→ 撮合过程中价格超过 10,521 → 停止匹配
→ 已成交部分正常输出，剩余取消
```

---

## 四、为什么撮合不能拆分，以及性能怎么优化

### 为什么不能拆

```
━━━━ 单实例撮合（正确）━━━━

                 ┌──────────────────────────┐
  Order-P ──────▶│                          │
  (BUY 1 BTC)    │   Matching Engine        │  Orderbook:
                 │   单一实例                │  Order-F: SELL 0.5 BTC @ 10,020
  Order-Q ──────▶│                          │
  (BUY 0.8 BTC)  │   逐笔处理                │
                 └────────────┬─────────────┘
                              │
                     先处理 P，再处理 Q
                              │
                 结果：P 买到 0.5 BTC（Order-F 全部成交）
                       Q 无法匹配（Order-F 已被消耗）→ 挂单
                       总成交: 0.5 BTC ✅

━━━━ 多实例撮合（错误 ❌）━━━━

                 ┌──────────────────────────┐
  Order-P ──────▶│  Matching Engine #1      │  各自维护 Orderbook 副本
  (BUY 1 BTC)    │  看到 Order-F: 0.5 BTC    │  Order-F: SELL 0.5 BTC
                 │  → P 成交 0.5 BTC         │
                 └──────────────────────────┘

                 ┌──────────────────────────┐
  Order-Q ──────▶│  Matching Engine #2      │  各自维护 Orderbook 副本
  (BUY 0.8 BTC)  │  看到 Order-F: 0.5 BTC    │  Order-F: SELL 0.5 BTC
                 │  → Q 也成交 0.5 BTC       │
                 └──────────────────────────┘

  结果：P 买到 0.5 + Q 买到 0.5 = 总卖出 1.0 BTC
        但 Order-F 只有 0.5 BTC ← 状态矛盾！
        本质上是"双花"问题 — 同一笔资产被卖了两次
```

> 与第 1 节 Account Service 并发冻结超额下单的问题本质相同：共享状态的并发读写，没有串行化就会不一致

---

### 按交易对分片

```
┌──────────────────────────────────────────────────────────┐
│                  Matching Engine 集群                      │
│                                                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │
│  │  BTC/USDT   │  │  ETH/USDT   │  │  SOL/USDT   │      │
│  │  Matching   │  │  Matching   │  │  Matching    │ ... │
│  │  Engine     │  │  Engine     │  │  Engine      │      │
│  │             │  │             │  │             │      │
│  │ 独立         │  │ 独立        │  │ 独立        │      │
│  │ Orderbook   │  │ Orderbook   │  │ Orderbook   │      │
│  └─────────────┘  └─────────────┘  └─────────────┘      │
│                                                           │
│  同一交易对不能拆，跨交易对可以分                            │
│  跨交易对操作（资金共享/套利检测）在撮合之外协调              │
└──────────────────────────────────────────────────────────┘
```

---

### 性能优化方向

**撮合引擎优化：**

| 优化方向 | 具体做法 | 效果 |
|---------|---------|------|
| **按交易对分片** | 每个交易对独立撮合引擎实例，并行运行 | 整体吞吐量随交易对数量线性扩展 |
| **内存撮合** | 整个 Orderbook 驻留在内存中，不走数据库 | 消除 IO 延迟 |
| **单线程 + 事件驱动** | 避免锁竞争，用事件队列保证顺序处理 | 无锁开销，单实例吞吐量最大化 |

**下游传输层优化（不在撮合引擎内部）：**

| 优化方向 | 具体做法 | 效果 |
|---------|---------|------|
| **异步写入事件队列** | Trade 逐笔写入 Ring Buffer，下游按自己节奏批量拉取 | 撮合线程不被下游阻塞 |
| **快照 + 增量** | Market Data 首次拿全量快照，之后只接收变化的档位 | 减少数据传输量 |

参考量级：头部交易所宣称的"百万级 TPS"是所有交易对汇总，单个交易对的单线程实例会低于这个数字

---

### 撮合引擎的高性能传输架构

```
Order Service → External MQ
                    │
    ┌───────────────┼──── 撮合引擎进程 ────────────────────┐
    │               ▼                                      │
    │      Network Receiver（IO 线程）                      │
    │               │                                      │
    │               ▼                                      │
    │      Input Ring Buffer                               │
    │               │                                      │
    │               ▼                                      │
    │      Matching Engine（撮合线程）                       │
    │               │                                      │
    │               ▼                                      │
    │      Output Ring Buffer                              │
    │               │                                      │
    │               ▼                                      │
    │      Network Publisher（IO 线程）                      │
    │               │                                      │
    └───────────────┼──────────────────────────────────────┘
                    │
                    ▼
               External MQ
                    │
          ┌────────┴────────┐
          ▼                 ▼
   Clearing Service   Market Data Service
```

- **Ring Buffer**：进程内无锁队列，延迟 ~ns 级（vs External MQ ~ms 级）
- **Network Receiver / Publisher**：独立 IO 线程，桥接"进程内队列"与"外部 MQ"
- **撮合线程不做 IO** — 只从 Input Ring Buffer 读、向 Output Ring Buffer 写
- **上下游服务通过 External MQ 通信** — 不需要与撮合引擎在同一台服务器
- **为什么单线程而非多线程 + 锁？** — Orderbook 是全局共享状态，不可按用户分区。对比 Account Service 可以用行锁（状态按用户隔离），撮合用单线程彻底消除并发问题

> 高性能撮合的核心：撮合线程永远不等待 IO。Ring Buffer 隔离了 μs 级撮合与 ms 级网络之间的速度差异。

---

### 撮合引擎的高可用 — Primary-Standby

```
同一个 MQ Topic (orders.BTC-USDT)
    ├──▶ 服务器 A（Primary）── 输出 Trade ✅
    ├──▶ 服务器 B（Standby） ── 不输出 ❌
    └──▶ 服务器 C（Standby） ── 不输出 ❌

三台消费相同输入、维护相同 Orderbook，只有 Primary 输出
```

- **为什么可行**：确定性 — 相同输入 + 相同顺序 = 相同 Orderbook 状态
- **故障切换**：Standby 立即接管，交易接近零停摆
- **故障恢复**：从活着的节点拿快照 + 重放 MQ 追平 → 重新加入

> 确定性不仅是正确性的基础，也是高可用的基础。

---

## 五、撮合引擎的职责边界

### 撮合引擎只做三件事

| 职责 | 说明 |
|------|------|
| 维护 Orderbook | 接收新订单插入、处理撤单移除 |
| 执行撮合匹配 | 按 Price-Time Priority 逐级匹配，输出 Trade |
| 输出事件 | 将 Trade、Orderbook 变化推送给下游服务 |

### 撮合引擎不做的事

| 不做的事 | 由谁做 | 为什么不在撮合里做 |
|---------|-------|------------------|
| 余额检查 / 保证金检查 | Risk Service（Pre-trade） | 风控逻辑变化频繁，不应耦合到撮合核心 |
| 资金冻结 / 解冻 | Account Service | 撮合引擎不持有资金状态 |
| 成交结算 / 资金划转 | Clearing Service | 结算涉及手续费、账本等，逻辑复杂 |
| 仓位创建 / 更新 | Position Service | 撮合不区分现货和合约 |
| K 线生成 / 行情推送 | Market Data Service | 行情是读路径，撮合是写路径 |
| 强平决策 | Risk Service + Liquidation Engine | 强平是风控行为，不是撮合行为 |

> 撮合引擎的纯粹性就是它的速度来源

---

### 撮合引擎输入/输出边界

```
输入:
  合法订单（已通过冻结+风控）← Order Service
  撤单请求                ← Order Service
  强平单                  ← Liquidation Engine

        ★ Matching Engine
        维护 Orderbook · 执行匹配 · 输出事件

输出:
  Trade 事件     → Clearing Service（结算·资金划转）
  Orderbook 变更 → Market Data Service（深度·行情推送）

不经过撮合的路径:
  Funding 结算 → Account Service（直接操作余额）
  内部划转      → Account Service（直接操作余额）
  充值入账      → Wallet → Account Service
```

---

### 延迟量级感知

```
纯内存撮合（查找+匹配）:  ~1-10 μs
如果加入余额检查:        +50-500 μs（网络调用 Account Service）
如果加入风控判断:        +100-1000 μs（多条件查询）
如果加入结算逻辑:        +200-2000 μs（持久化写入）

纯撮合 ~10 μs vs 加入所有职责 ~2500+ μs → 延迟增大 250 倍
```

> 撮合引擎的边界 = 它的速度。边界越清晰，引擎越快。

---

### 面试问题

**"撮合引擎和风控是怎么配合的？"**

- 好答案：风控在撮合之前（Pre-trade），撮合本身不做风控。撮合之后由 Clearing 触发结算。撮合只负责"在合法订单集合中按规则匹配"。
- 差答案：撮合引擎里会检查保证金够不够... ← 对系统边界不清楚

---

### 四个判断点

1. 撮合是系统的"单点"，但这个单点是有意为之的 — 裁决必须唯一
2. 不要试图消灭这个瓶颈，而是让它足够快、足够纯粹
3. 如果一个系统设计允许撮合被绕过或分裂，那这个系统一定存在一致性隐患
4. 撮合引擎的边界 = 它的速度。边界越清晰，引擎越快

---

### 核心命题回扣：为什么说"撮合引擎必须是系统中心"

1. **"中心"不是说它最复杂** — 恰恰相反，撮合的代码逻辑可能是核心服务中最简单的。"中心"是指它的**定序权**不可替代。
2. **撮合是唯一的全局定序点** — 所有交易最终由它裁决成交顺序和成交价格，这个裁决权不能分享、不能延迟、不能重复。
3. **撮合的纯粹性是整个系统性能的关键** — 它越简单、越快，系统的交易延迟就越低。
4. **与第 1 节的关系** — 资金系统提供了正确的输入（冻结确认 + Pre-trade 通过），撮合在这些合法输入上做裁决，输出给结算系统完成资金划转。三者构成交易链路的核心闭环。

---

**下节预告：** 撮合保证了"成交的确定性"，但系统运行久了一定会出错 — 出错之后靠什么兜底？→ 第 3 节：账本（Ledger）
