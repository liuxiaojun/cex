# 第 3 节 | 账本（Ledger）为什么一定是系统兜底

> **核心命题：** 账本不是"记账工具"，而是系统的最后防线。当一切都可能出错时，账本是唯一可以回溯和自证的东西。

---

## 本节要回答的问题

| 序号 | 问题 |
|------|------|
| 一 | "余额对了"是不是就意味着"账对了"？ |
| 二 | 账本条目的数据结构长什么样？复式记账的核心约束是什么？ |
| 三 | 不同业务场景（成交、强平、Funding、充提）在账本里留下的记录有什么不同？ |
| 四 | 交易→账本→清算的闭环是怎么运转的？断了怎么办？对账怎么做？ |
| 五 | 账本在系统中的定位是什么？为什么它必须独立且不可篡改？ |

---

## 一、为什么余额不等于账

**开场衔接：** 上节课 — "成交的确定性"。这节课 — "出错之后靠什么兜底"。

---

### 余额 vs 账本

```
┌─────────────────────────────────────────────────────────────────────┐
│                      余额 vs 账本                                    │
│                                                                     │
│  ┌─── 余额（Balance）─────────────────────────────────────────┐     │
│  │                                                             │     │
│  │  Account Service 中的一个字段:                               │     │
│  │                                                             │     │
│  │    user_A / USDT / Available = 8,500                        │     │
│  │                                                             │     │
│  │  特点:                                                      │     │
│  │    ✓ 实时更新 — 每次成交/充值/提现后立刻变化                  │     │
│  │    ✓ 查询极快 — 一次 SELECT 就能拿到                         │     │
│  │    ✗ 只有"当前值" — 看不到历史                               │     │
│  │    ✗ 无法自证 — 不知道这个 8,500 是怎么来的                   │     │
│  │    ✗ 被改了也不知道 — 如果有人直接改数据库，无法发现            │     │
│  │                                                             │     │
│  └─────────────────────────────────────────────────────────────┘     │
│                                                                     │
│  ┌─── 账本（Ledger）──────────────────────────────────────────┐     │
│  │                                                             │     │
│  │  Ledger Service 中的一系列条目:                               │     │
│  │                                                             │     │
│  │    #001  DEPOSIT   +10,000 USDT   (充值)                    │     │
│  │    #002  TRADE     -1,500 USDT    (买入 BTC)                │     │
│  │    #003  FEE       -5 USDT        (手续费)                  │     │
│  │    #004  FUNDING   +5 USDT        (Funding 收入)            │     │
│  │    ────────────────────────────                             │     │
│  │    累加结果 = 10,000 - 1,500 - 5 + 5 = 8,500 ✓              │     │
│  │                                                             │     │
│  │  特点:                                                      │     │
│  │    ✓ 完整过程 — 每一笔变动都有记录                            │     │
│  │    ✓ 可重算 — 从零累加，一定能算出当前余额                     │     │
│  │    ✓ 可自证 — 每条记录有 ref_id，可追溯到原始业务              │     │
│  │    ✓ 不可篡改 — append-only，改了就能被发现                   │     │
│  │                                                             │     │
│  └─────────────────────────────────────────────────────────────┘     │
│                                                                     │
│  关系: 余额 = 账本所有条目的累加结果                                 │
│        余额是"结果"，账本是"证据"                                    │
│        结果可以随时从证据重新算出                                     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**类比：** 余额是"温度计读数"，账本是"完整的天气记录"。温度计可以坏，但天气记录完整就能重新校准。

---

### 案例：余额异常无法追溯

某中小型交易所运营初期没有独立的 Ledger Service，所有余额变动直接修改 Account Service 的 Balance 字段。

运营三个月后，运营团队发现某用户的 USDT 余额异常偏高 — 比该用户的充值总额加上交易盈利的合理估算高出约 5 万 USDT。

**排查过程：**
1. 查 Account Service → 只能看到当前余额，看不到历史变动
2. 查 Trade 记录 → 只能看到成交，看不到充值、提现、划转等其他变动
3. 查 Deposit 记录 → 只能看到充值，看不到交易和费用
4. 试图把所有来源拼凑起来 → 数据分散在不同服务，时间戳对不齐，无法确认是否有遗漏

**结果：** 花了两周人力逐笔对照，最终发现是一次系统升级时，Clearing Service 的一个 bug 导致某些成交的结算金额被重复写入。但因为没有统一的账本记录，无法确认还有多少用户受到影响。

**核心教训：** 余额只是结果，没有过程记录就无法追溯。系统越复杂、服务越多，"从各个来源拼凑还原"越不可行。

---

### 账本的可重算性 — "从头算出正确结果"

"从头算出正确结果"不是比喻，而是账本最核心的设计目标：

1. **重算**：把某用户的所有账本条目按时间排列，逐条累加 CREDIT、减去 DEBIT，得到的结果就是该用户的"正确余额"
2. **对账**：将重算结果与 Account Service 的实时余额比对 — 如果一致，说明系统正常；如果不一致，说明某个环节出了问题
3. **修复**：通过账本的 ref_id 追溯到具体的业务事件（哪笔成交、哪笔充值），定位问题根源

这就是为什么第 1 节中提到的 Available / Frozen / Equity 是"实时快照"，而账本是这些快照的**证据链**。快照可以重建，但证据链不能中断。

> 余额是"温度计"，账本是"天气记录"。温度计可以坏，但只要天气记录完整，你随时可以重新校准。

---

## 二、Ledger 的数据模型

### 账本条目结构

```
LedgerEntry:
    id:            唯一流水号（全局递增，不可重复）
    biz_type:      业务类型（TRADE / DEPOSIT / WITHDRAW / FUNDING / LIQUIDATION / FEE / TRANSFER）
    ref_id:        关联业务 ID（成交ID / 充值ID / 强平ID 等）
    entries:       [
        { account: "user_A", asset: "USDT", direction: DEBIT,  amount: 1000 },
        { account: "user_B", asset: "USDT", direction: CREDIT, amount: 1000 },
    ]
    timestamp:     创建时间（不可修改）
    status:        CONFIRMED（不可回滚）
```

| 字段 | 含义 | 为什么必须有 |
|------|------|-------------|
| id | 全局唯一流水号 | 保证每条记录可寻址、不重复 |
| biz_type | 业务类型 | 区分不同场景，对账时可按类型分类汇总 |
| ref_id | 关联业务 ID | 追溯到原始业务事件（成交、充值等） |
| entries | 借贷明细数组 | 一笔业务可能涉及多个账户、多个资产 |
| timestamp | 创建时间 | 时间线还原，不可修改 |
| status | 状态 | 一旦 CONFIRMED 就不可回滚 |

---

### 复式记账核心约束

**每一笔记录中，DEBIT 总和 == CREDIT 总和（按每种 asset 分别成立）**

```
现货成交:
  USDT 维度: DEBIT 10,000 (买方付出) == CREDIT 10,000 (卖方收到) ✅
  BTC 维度:  DEBIT 1 (卖方付出) == CREDIT 1 (买方收到) ✅

如果任何一条记录的 DEBIT ≠ CREDIT → 立刻发现有错
所有记录的 DEBIT 总和 == CREDIT 总和 → 整个系统的资金守恒

→ 这个约束就是账本能"自证"的数学基础
```

---

### append-only + 冲正

**append-only：** 已写入的记录永远不能修改、不能删除。

| 属性 | 允许修改 | append-only |
|------|---------|-------------|
| 可审计性 | 无法证明历史没被篡改 | 每条记录有独立时间戳和 ID，可追溯 |
| 可重放性 | 修改后无法重算 | 从第一条记录累加即可重算任何时点的余额 |
| 错误发现 | 错误被"修正"后消失 | 冲正记录本身就是"错误曾经发生"的证据 |
| 合规审计 | 监管无法验证         | 监管可以独立验证账本完整性 |

**冲正流程：**

```
原始错误 #005: user_A DEBIT 10,000（应该是 1,000）

第 1 步 — 冲销: #010 user_A CREDIT 10,000（抵消原来的 DEBIT）
第 2 步 — 补记: #011 user_A DEBIT 1,000（写入正确金额）

净效果: -10,000 + 10,000 - 1,000 = -1,000 ✅
且 #005 仍然存在 → 完整审计轨迹
```

```
correct_error(original_entry):
    append(reverse(original_entry))    // 先冲销原记录
    append(corrected_entry)            // 再写入正确记录
```

### 案例：直接改数据库导致对账崩溃

某交易所在一次手续费计算错误后，DBA 直接通过 SQL 修改了 Ledger 表中约 200 条记录的金额字段，将错误的手续费金额改为正确值。

**后果：**
1. 当天对账时，对账脚本发现大量"差异" — 因为 Account Service 的余额是基于**原始（错误的）**金额实时更新的，而 Ledger 中的金额已经被改成了**正确值**
2. 对账脚本按修改后的 Ledger 累加余额，结果和 Account Service 的实时余额对不上
3. 运营团队无法判断这些差异是"真正的系统 bug"还是"DBA 改数据的副作用" — 因为历史记录已经被篡改，无法区分
4. 最终花了三天时间，从 Account Service 的余额变更日志（如果有的话）反向推算，才确认了影响范围

**根本原因：** 账本的 append-only 约束被绕过了。一旦历史记录可以被修改，账本就失去了"自证"能力 — 你无法确认当前看到的记录是否就是当初写入的。

**正确做法：** 对这 200 条记录逐条写冲正记录 + 补记正确记录。工作量更大，但保证了账本的完整性和可审计性。

> 任何导致资金变化的操作，必须在账本中有对应条目。如果发现"余额变了但账本里没记录"，系统一定有漏洞。

---

## 三、不同业务场景的账本记录

### 业务场景 → 账本条目汇总表

| biz_type | 场景 | 涉及账户 | 借方（DEBIT） | 贷方（CREDIT） |
|----------|------|---------|-------------|---------------|
| TRADE | 现货成交 | user_A, user_B | 买方付出 USDT + 卖方付出 BTC | 买方收到 BTC + 卖方收到 USDT |
| FEE | 手续费 | buyer, seller, platform_fee | 买卖双方各自扣除手续费 | 平台收入 |
| TRADE | 合约开仓 | user（内部划转） | available 减少 | margin 增加 |
| TRADE | 合约平仓 | user, counterparty | 释放保证金 + 亏损方扣款 | 盈利方收款 |
| LIQUIDATION | 强平/穿仓 | user, counterparty, insurance_fund | 没收保证金 + 保险基金补亏 | 对手方收款 |
| FUNDING | Funding Rate | 所有持仓用户（按多/空方向） | 付出方按持仓价值扣款（零和） | 收入方按持仓价值到账 |
| DEPOSIT | 充值 | external, user | 外部来源 | 用户到账 |
| WITHDRAW | 提现 | user, external | 用户扣款 | 转出到链上 |

---

### 七种场景账本条目

**现货成交**
```
biz_type: TRADE, ref_id: trade_001
entries:
    user_A  USDT  DEBIT   10000     // 买方付出 USDT
    user_B  USDT  CREDIT  10000     // 卖方收到 USDT
    user_A  BTC   CREDIT  1         // 买方收到 BTC
    user_B  BTC   DEBIT   1         // 卖方付出 BTC
```

**手续费** — 必须单独记账，不能混在成交条目里
```
biz_type: FEE, ref_id: trade_001
entries:
    user_A        BTC   DEBIT   0.001     // 买方手续费（从收到的 BTC 中扣）
    platform_fee  BTC   CREDIT  0.001     // 平台收入
    user_B        USDT  DEBIT   10        // 卖方手续费（从收到的 USDT 中扣）
    platform_fee  USDT  CREDIT  10        // 平台收入
```

**合约开仓（保证金划转）** — 同一用户内部划转也要记账本（可用性变了）
```
biz_type: TRADE, ref_id: trade_002
entries:
    user_A  USDT         DEBIT   1000      // 保证金从 available 划入 margin
    user_A  USDT_MARGIN  CREDIT  1000
```

**合约平仓（盈利）** — 保证金释放与盈亏结算是两件独立的事
```
biz_type: TRADE, ref_id: trade_003
entries:
    // 保证金释放（user_A 内部划转）
    user_A  USDT_MARGIN  DEBIT   1000   // 释放保证金
    user_A  USDT         CREDIT  1000
    // 盈亏结算（盈利 500，取决于价差，与保证金金额无关）
    user_B  USDT         DEBIT   500    // 对手方亏损
    user_A  USDT         CREDIT  500    // 当前用户盈利
```

**强制平仓（穿仓）** — 保险基金也是账本中的"账户"
```
biz_type: LIQUIDATION, ref_id: liq_001
entries:
    // 保证金释放（user_A 内部划转）
    user_A          USDT_MARGIN  DEBIT   1000   // 解除保证金锁定
    user_A          USDT         CREDIT  1000   // 转回可用
    // 赔付对手方（保证金全额赔付）
    user_A          USDT         DEBIT   1000   // 保证金全部赔付
    user_B          USDT         CREDIT  1000   // 对手方获得
    // 穿仓部分由保险基金补足（亏损 1040 - 保证金 1000 = 缺口 40）
    insurance_fund  USDT         DEBIT   40     // 保险基金承担穿仓差额
    user_B          USDT         CREDIT  40     // 对手方获得补偿
```

**Funding Rate 结算** — 每 8 小时对所有持仓用户批量结算，不经过撮合引擎
```
// 假设当期 Funding Rate 为正（多头付空头）
biz_type: FUNDING, ref_id: funding_round_42
entries:
    user_A(long)   USDT  DEBIT   12     // 多头 A 付出
    user_B(long)   USDT  DEBIT   5      // 多头 B 付出
    user_C(short)  USDT  CREDIT  10     // 空头 C 收入
    user_D(short)  USDT  CREDIT  7      // 空头 D 收入
    // 多头总付出 17 == 空头总收入 17（零和）
```

**充值 / 提现** — `external` 是系统边界账户
```
// 充值
biz_type: DEPOSIT, ref_id: deposit_001
entries:
    external       USDT  DEBIT   5000
    user_A         USDT  CREDIT  5000

// 提现
biz_type: WITHDRAW, ref_id: withdraw_001
entries:
    user_A         USDT  DEBIT   3000
    external       USDT  CREDIT  3000
```

> 不管业务多复杂，账本条目的结构是统一的 — 都是 `biz_type + ref_id + entries[]`。这种统一性就是账本能"兜底一切"的原因。

---

## 四、交易→账本→清算的闭环与对账

### 闭环链路

```
┌────────────────────────────────────────────────────────────────────────┐
│                    交易→账本→清算 闭环链路                                │
│                                                                        │
│  ╔══════════════════╗                                                  │
│  ║  ★ Matching      ║                                                  │
│  ║    Engine        ║                                                  │
│  ╚═══════╤══════════╝                                                  │
│          │                                                             │
│          │ 输出 Trade 事件                                              │
│          │ (trade_id, buyer, seller, price, qty)                        │
│          │                                                             │
│          ▼                                                             │
│  ┌───────────────────────────────────────────────────────────────┐     │
│  │                   Clearing Service                            │     │
│  │                                                               │     │
│  │   1. 解析 Trade 事件                                          │     │
│  │   2. 计算结算金额（含手续费）                                  │     │
│  │   3. 同时执行以下两个操作：                                    │     │
│  │                                                               │     │
│  │   ┌─────────────────────────┐  ┌──────────────────────────┐   │     │
│  │   │  写入 Account Service   │  │  写入 Ledger Service     │   │     │
│  │   │                         │  │                          │   │     │
│  │   │  • 买方 USDT 扣款       │  │  • biz_type: TRADE      │   │     │
│  │   │  • 卖方 USDT 入账       │  │  • ref_id: trade_001    │   │     │
│  │   │  • 买方 BTC 入账        │  │  • entries: [DEBIT/     │   │     │
│  │   │  • 卖方 BTC 扣款        │  │    CREDIT 明细]          │   │     │
│  │   │  • 解冻 Frozen          │  │                          │   │     │
│  │   │  • 买方扣 BTC 手续费    │  │  • biz_type: FEE        │   │     │
│  │   │  • 卖方扣 USDT 手续费   │  │  • ref_id: trade_001    │   │     │
│  │   │  • platform_fee 入账    │  │  • entries: [手续费明细]  │   │     │
│  │   └─────────────────────────┘  └──────────────────────────┘   │     │
│  │                                                               │     │
│  │   关键约束: 两个写入必须同时成功或同时失败                       │     │
│  │             (事务保证 / 最终一致性)                              │     │
│  └───────────────────────────────────────────────────────────────┘     │
│                                                                        │
│  ┌───────────────────────────────────────────────────────────────┐     │
│  │                   持续对账                                     │     │
│  │                                                               │     │
│  │   Account Service 余额  ←──对比──→  Ledger 条目累加           │     │
│  │                                                               │     │
│  │   一致 → ✅ 系统正常                                          │     │
│  │   不一致 → 🚨 报警，按 ref_id 追溯                             │     │
│  └───────────────────────────────────────────────────────────────┘     │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

---

### 闭环断裂三种情况

```
情况 1: 余额变了，账本没写
  → Account ✅, Ledger ❌ → 用户余额已变化，但无据可查

情况 2: 账本写了，余额没更新
  → Account ❌, Ledger ✅ → 用户余额没变，但账本显示交易发生了
  → 修复: 根据 Ledger 补刷余额（以账本为准）

情况 3: 两者都写了，但数据不一致
  → Account 金额 X, Ledger 金额 Y → 对账发现差异
  → 修复: 以 Ledger 为准，修正 Account 余额

三种情况都能通过持续对账发现 — 前提是账本必须存在且不可篡改
```

---

### Clearing 的原子性保证

"Clearing 必须同时写余额和写账本" — 具体怎么保证？

**常见方案：**

1. **本地事务 + 同库**：Account 表和 Ledger 表在同一个数据库中，使用数据库事务保证原子性。适合中小规模系统。

2. **两阶段提交（2PC）**：Account Service 和 Ledger Service 是独立服务，使用分布式事务协调。延迟较高，但强一致。

3. **最终一致性 + 补偿**：先写 Ledger（因为 append-only，写入成功就不会丢），再写 Account。如果 Account 写入失败，通过异步补偿重试。持续对账作为兜底检查。

行业实践中，方案 3 最常见 — 核心思路是："宁可余额暂时不准，也不能丢失账本记录" — 因为只要账本完整，余额随时可以重算。

---

### 对账：三层验证体系

传统金融有"收盘时间"，系统静止后可以安全取快照做批量对账。但加密市场 24/7 全球交易，没有"收盘"概念。基于这个约束，对账需要从"单次批量比对"转变为**分层持续验证**。以下是一种合理的设计思路：

```
第一层：实时逐笔校验（毫秒级）
  每笔 Clearing 完成后立刻校验 Account 和 Ledger 写入是否一致
  → 不一致立刻报警，不等周期对账
  → 主要防线

第二层：按 business_hour 增量对账（小时级）
  借鉴传统金融 business_date 思路，粒度从"天"缩小到"小时"：
  → 每笔 Clearing 事件打上 business_hour 标签（如 2025-01-15T14）
  → Account Service 每小时记录余额快照到历史表
  → 对账已关闭小时的增量：
    ledger_delta  = sum(business_hour=14 的 CREDIT - DEBIT)
    account_delta = snapshot[15:00] - snapshot[14:00]
  → 小时切换时设缓冲窗口（如 5 分钟），等 in-flight 事件落库后再对账
  → 增量对账，不需全量重算；问题定位缩小到"某一小时"

第三层：ref_id 交叉比对（小时级）
  逐条匹配，不依赖余额总数：
  → Ledger 有记录但 Account 没变更 → 漏更新
  → Account 有变更但 Ledger 没记录 → 漏记
  → 不受 in-flight 影响

核心思路：不是"找安静时间点做快照比对"
         而是"持续验证，用多轮确认过滤 in-flight 噪声"
```

- 发现确定差异 → 按 ref_id 回溯 → 定位是"漏记"还是"漏更新" → 冲正 + 补记修复

---

## 五、账本的系统定位

### 系统的"黑匣子"

- 平时感觉不到它的存在 — 日常运营靠 Account Service 的实时余额
- 出事时它是唯一的救命稻草 — 余额可能错，账本一定能还原真相

---

### 自证与审计

```
监管审计: "你能证明每一分钱的来源和去向吗？"
→ 拉出 Ledger，按 asset 分组，验证 DEBIT == CREDIT → 资金守恒

用户投诉: "我的钱怎么少了？"
→ 按 user_id 拉出所有账本条目，逐笔展示

实时余额不能作为证据 — 它只是结果，不是过程
```

---

### "以账本为准"

```
发现差异 → 以 Ledger 累加值为准
         → 通过 ref_id 定位具体业务事件
         → 修正 Account 余额（而不是修改 Ledger）
```

---

### 账本位置全景图

```
Order Service → 冻结+风控 → MQ → ★ Matching Engine → Clearing Service
                                                          │
                                               ┌─────────┴─────────┐
                                               ▼                    ▼
                                        Account Service      ★ Ledger Service
                                        (更新余额)            (写入账本)

不经过撮合也写账本:
  Funding 结算 / 内部划转 / 充值 / 提现 → Account + Ledger

持续对账: Account 余额 ←─ 对比 ─→ Ledger 累加
         一致 → ✅  |  不一致 → 🚨 以 Ledger 为准
```

---

### DEX 预告

在 DEX 场景下，区块链本身就是一个公开的、不可篡改的、append-only 的 Ledger — 第 6 节展开。

---

### 四个判断点

1. 账本是系统的"最终真相" — 余额和账本不一致时，以账本为准
2. 账本必须 append-only，修正错误靠冲正，不靠改历史
3. 所有业务场景的账本条目结构是统一的 — 这种统一性是兜底能力的来源
4. 如果一个系统没有独立的、不可篡改的、可对账的账本层，它无法长期安全运行

---

### 核心命题回扣：为什么说"账本一定是系统兜底"

1. **余额是快照，账本是证据链** — 快照可以重建，但证据链不能中断
2. **复式记账的数学约束让账本能自证** — DEBIT == CREDIT 验证资金守恒
3. **append-only + 冲正保证了不可篡改性** — 历史永远完整
4. **统一的条目结构覆盖一切业务场景** — 扩展新业务只需定义新 biz_type
5. **与第 1 节的关系** — Account Service 的余额字段是快照，Ledger 是证据链
6. **与第 2 节的关系** — 撮合输出 Trade → Clearing 同时写 Account + Ledger → 持续对账验证闭环

---

**下节预告：** 账本能帮你"事后追溯"，但有些风险一旦发生就不可逆 — 这就是为什么风控必须"嵌在流程中"。→ 第 4 节：风控为什么必须嵌在交易流程中
